# Sound Radar — 落地设计（Windows · 暗区突围：无限 · 图标悬浮窗 · 可编辑向量库）

> 需求已锁定：**只做 Windows** / **游戏：暗区突围：无限** / **命中后在悬浮窗展示图标** /
> **能在软件界面里查看、修改、录入向量（上传音频 + 起名 + 配图标）**。
> 前置文档：[`方案.md`](方案.md)（语言无关的识别原理）、[`方案-go实现.md`](方案-go实现.md)（Go/体积/依赖选型）。
> 本文是**可开工的实现规格**。

---

## 1. 总体架构：一个 exe，三块

```
soundradar.exe
├─ ① 核心运行时（无 UI，纯 Go）
│     WASAPI loopback 采集 → 环形缓冲 → log-mel 特征 → 量化 → 扁平检索 → 事件去抖
│     └─ 只读系统音频输出，不碰游戏进程
├─ ② 悬浮窗（原生 Win32 分层窗口）
│     命中即显示：图标 + 名称 + 置信度环；置顶、鼠标穿透、不抢焦点
└─ ③ 管理端（**独立窗口**：内嵌 HTML 由 WebView2 渲染，页面由内嵌的 127.0.0.1 服务提供）
      库列表 / 条目编辑 / 上传音频录入 / 配图标 / 实时相似度打分 / 离群清理 / 设置
```

三块在同一个进程里，通过内存事件总线通信；管理端用 `net/http` + `embed` 提供页面，
**不引入任何 Go GUI 框架**。

---

## 2. 三个关键工程决策（先说清为什么）

| 决策 | 选择 | 理由 |
|---|---|---|
| 悬浮窗怎么做 | **原生 Win32 分层窗口**（windigo 或裸 syscall） | 悬浮窗常驻、要求低延迟、低占用、不能挡鼠标 → `WS_EX_TRANSPARENT` 鼠标穿透 + `UpdateLayeredWindow` 逐像素透明，零额外体积 |
| 管理端怎么做 | **独立窗口 + WebView2**（页面仍是内嵌 HTML，服务仍是 `net/http`+`embed`） | 管理端需要：拖拽上传、音频试听、图片预览、表格编辑、实时分数条。用 HTML5 全免费：`<audio>` 直接试听、`FormData` 直接上传、CSS 直接画分数条。窗口壳用**纯 Go 的 WebView2 绑定**（无 cgo），不引 GUI 框架 |
| 兜底 | WebView2 运行时缺失时，自动退回**系统默认浏览器**打开同一地址 | 逻辑不变、零成本；不会因为系统组件缺失而整个功能不可用 |

> 拒绝的方案：Fyne/Gio（+8–20 MB，违背"小工具"）、Wails/Lorca（至少一边要 cgo）、
> 手写一堆 Win32 通用控件做管理界面（代码量 10 倍，收益为负）。

### 2.1 WebView2 spike 实测结论（2026-02）
用 `github.com/jchv/go-webview2 v0.0.0-20260205173254-56598839c808`（纯 Go，`CGO_ENABLED=0`、Go 1.27.1）实测：

| 项 | 结果 |
|---|---|
| 编译 | ✅ 成功，**无 cgo、无外部 DLL**（内嵌 `WebView2Loader.dll` 137,640 B + go-winloader 内存加载） |
| WebView2 运行时 | ✅ 已装 `153.0.4234.32`（注册表 `HKLM\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-…}`）。**注意：运行时目录里没有 `WebView2Loader.dll`**，"装了 Runtime 就有 Loader"是错的 → 内嵌 Loader 是必需的 |
| 窗口 | ✅ 真实 Win32 顶层窗口，HWND 可拿、标题可设 |
| 精确定位 | ✅ `SetWindowPos(120,80,900,600)` → `GetWindowRect` 实读 `L120 T80 R1020 B680`，**零偏差** |
| 任务栏 | ✅ 样式 `0x14CF0000`、无 `WS_EX_TOOLWINDOW`、无 owner → 具备任务栏资格 |
| DPI | `PerMonitorV2` 设置成功；本机 100%，**坐标一律物理像素** |
| 页面渲染 | ⚠️ **未能在本开发沙箱内取得证据**：`CreateCoreWebView2Controller` 返回 `0x8000FFFF`。对照实验证明是沙箱所致——**独立的 Microsoft Edge 在同一沙箱里以同样的 `mojo platform_channel ERROR_ACCESS_DENIED(0x5)` 失败**，且 token 只剩 `SeChangeNotifyPrivilege`。提权到 full-access 后 **controller 创建成功**（`create_ok=true`），但渲染进程仍起不来 → 需在**普通用户会话**里跑一次 `spike\webview2\spike_webview2.exe` 才能定论 |

代价与对策：
- **体积**：`-s -w` 后 6.92 MB，基线（`net/http`+`embed`）5.45 MB → WebView2 壳约 **+1.5 MB**。
  若在意体积，可把 localhost 服务换成手写的最小 HTTP/1.1（丢掉 `net/http` 拉进来的 `crypto/tls`/`x509`），能省 1–2 MB。
- **必须有兜底**：WebView2 建窗口失败或页面 N 秒内没有 ready 回调 → **自动退回系统默认浏览器**打开同一地址。
  这样管理端在任何机器上都不会不可用，WebView2 只是"更好看的壳"。
- **库有真 bug**：`pkg/edge/chromium.go:187` 的 `if int64(res) < 0` 对 32 位 HRESULT 恒不成立（x64 ABI 零扩展），
  失败码被当成成功 → 解引用 NULL **裸 panic**。所以**必须**自己 `recover()` 包住 `NewWithOptions`。
- 其余五条必做项：启动先 `GetInstalledVersion()` 预检、**显式指定 `WebViewOptions.DataPath`**（默认 `%AppData%\<exe名>`，不可写就建不出来）、
  `-H=windowsgui`（否则多一个控制台窗口）、`Terminate()` 必须经 `Dispatch` 调用（它是 `PostQuitMessage`，线程本地）、建窗口前自己设 DPI 感知。

---

## 3. 库文件格式：**原始素材永远保留**

两个文件，职责分离：

### 3.1 `library.srz` —— 可编辑素材库（ZIP 容器，`archive/zip`）
```
manifest.json                      # 版本、特征参数、全局设置
items/<id>/meta.json               # 名称、图标、阈值、冷却、装备档案、备注
items/<id>/icon.png                # 图标（统一存 128×128 PNG，显示时缩放）
items/<id>/samples/0001.wav        # 原始样本（保留！）
items/<id>/samples/0002.wav
```
- 用 ZIP 的理由：stdlib 实现、单文件方便备份/分享/导入导出、**用任何解压软件都能打开看**、内嵌进 exe 只是一行 `//go:embed`。
- **样本原始音频必须留着**。因为将来改特征参数（mel 数、窗口长度、降维维度、量化位宽）后要**一键重算全部向量**；只存向量等于把数据做死了。这是这个格式最重要的一条。

### 3.2 `index.bin` —— 运行时索引（由库生成，秒级重建）
```
magic + 版本 + 特征参数指纹
类别表：id / 名称 / 图标在库里的偏移 / 阈值 / 冷却 / 装备档案
向量块：int8 量化，每类含 scale
```
- 启动时读入内存（典型 1600×128 dim int8 ≈ **200 KB**），暴力点积检索 **< 0.1 ms**。
- 特征参数指纹不匹配 → 自动提示重建（避免"改了参数却用旧索引"的隐蔽错判）。

### 3.3 `meta.json` 示例
```json
{
  "id": "a1b2c3",
  "name": "拾取物品",
  "icon": "icon.png",
  "threshold": 0.72,
  "cooldownMs": 400,
  "profile": "default",
  "tags": ["UI", "拾取"],
  "note": "搜包完成时听到的那声",
  "samples": [
    { "file": "samples/0001.wav", "offsetS": 0.12, "lenS": 0.60,
      "gainDb": 0.0, "source": "upload", "addedAt": "2026-01-01T12:00:00Z" }
  ]
}
```
- `samples` 是**多样本**：一个音效可以有近/中/远/隔墙等变体，匹配时取该类所有样本的最高分。
- `threshold` / `cooldownMs` **逐条可调**（不同音效的误报容忍度完全不同）。

### 3.4 `config.json` —— 应用级设置（和库分开）
```json
{
  "capture": { "device": "Realtek(R) Audio", "fallbackToDefault": true },
  "overlay": { "monitor": 0, "x": 1812, "y": 984, "size": 96, "opacity": 0.85, "durationMs": 1150, "anchor": "bottom-right" },
  "hotkeys": { "toggleOverlay": "F9", "recordLabel": "F8" },
  "profile": "default",
  "autoStart": false
}
```
- 库（`library.srz`）只放**识别数据**，配置放这里 —— 换库不影响你的窗口位置和端点选择，分享库也不会泄漏你的设置。
- `x/y` 为物理像素；`anchor` 只在 `x/y` 为空时生效（方便"想精确摆放"和"随手用默认"两种场景）。
- 改配置后**热生效**（悬浮窗立即移动、采集端点立即切换），不需要重启。

---

## 4. 管理端界面（这是你最常用的部分）

### 页面清单
1. **库列表**：图标网格 / 表格两种视图；显示名称、样本数、阈值、最近命中次数，支持搜索、标签筛选、批量重命名、合并、删除。
2. **条目详情**：
   - 波形缩略图 + **时频谱缩略图**（"可视化指纹"——你听不出，但"长得像不像"一眼能看出来）；
   - 样本清单，逐个可试听、可删除、可单独设增益/裁剪起止；
   - 阈值滑块 + 冷却时间；
   - 该条目的**实时相似度条**（见下）。
3. **录入向导**（三入口，见第 5 节）。
4. **实时打分面板（核心功能）**：
   一屏显示"当前这一瞬间"所有条目的相似度排行（前 10 名，带柱子），10–20 fps 刷新。
   → 戴上战术耳机去训练场走一圈、开一枪、开个门，**看哪根柱子飙起来就知道这条是什么声音**。
   这是给"听不出区别"的人的解药：把听觉辨识变成视觉辨识。
5. **离群样本清理**：自动标出"离该类中心最远"的样本，并列展示（可试听 + 看谱图）→ 一键剔除录错的样本。
6. **设置**：**采集端点下拉选择**（列出所有活动的渲染端点，可热切换，设备消失自动回退到默认）、灵敏度总开关、**悬浮窗坐标（X/Y 数值输入 + 显示器选择）**、悬浮窗大小/不透明度/显示时长、热键、装备档案。

### 技术要点
- 后端：`net/http`；实时数据用 **SSE（`text/event-stream`）** 推分数（比 WebSocket 简单，正好是单向流）。
- 上传：`multipart/form-data`，服务端转码/重采样到 48 kHz 单声道后入库。
- 音频格式支持：WAV（stdlib）、MP3（`hajimehoshi/go-mp3`）、OGG/Vorbis（`jfreymuth/oggvorbis`）、FLAC（`mewkiz/flac`）——都是**纯 Go 小库**；Wwise `.wem` 需先转 wav（见第 9 节）。
- 所有写操作落盘：改完立即重写 `library.srz` + 增量重建 `index.bin`（内容寻址，未变的样本不重算特征）。

---

## 5. 录入一个音效：三个入口

| 入口 | 流程 | 适合 |
|---|---|---|
| **A. 上传音频** | 拖入文件 → 自动切分候选段（能量/新颖度）→ 波形图上框选 → 试听 → 填名称 + 选图标 → 保存 | 从游戏 pak 提取出的原始音效、网上找的音效合集 |
| **B. 游戏中键标** | 全局热键（`RegisterHotKey`）→ 把最近 3 秒存成候选 → 管理端里显示波形分段 → 你选一段注册 | 只有实际玩的时候才听得到的声音（**ABI 的主力方式**） |
| **C. 批量导入** | 一次拖入整个文件夹 → 文件名当名称 → 自动生成占位图标（首字母 + 色块，按名称哈希上色）→ 你事后逐个换图标 | 拆包拿到几百个 .wav 时 |

**打标技巧（针对"听不出区别"）**：不要试图靠耳朵判断，靠**游戏里可观察的后果**打标——
"我按了这个键后出现的声音"=技能音，"弹夹数字变了"=换弹音，"屏幕红了"=受击音。
每次只针对一个事件反复触发 5 次，标签自然干净。配合第 4 节的实时打分面板，一条音效从"不知道是什么"到"命名+配图标"通常 1–2 分钟。

---

## 6. 识别层：以及 ABI 特有的四个坑（**这一节决定成败**）

### 6.1 两类声音必须区别对待

| 类型 | 例子 | 特性 | 方案 |
|---|---|---|---|
| **UI / 界面音** | 菜单、拾取、任务提示、结算、换弹 UI 音 | **每次播放完全一致** | L1 模板匹配，准确率极高，几乎零成本 |
| **世界 3D 音** | 脚步、枪声、开门、玻璃破碎、搜包、手雷 | **同一声源每次都不一样** | 必须做鲁棒化 + 变体库（见下） |

### 6.2 ABI 的四个坑与对策

1. **距离衰减**：远了不只是变小，高频先掉 → 频谱形状变了。
   → 特征按频带能量归一化，弱化绝对增益；同一条音效刻意注册"近/中/远"3 个变体。
2. **遮挡低通（隔墙/掩体）**：会叠一个低通滤波，听起来闷。
   → 注册"隔墙"变体；匹配器加一组低通增强样本。
3. **Wwise 的随机音高/音量**：脚步、枪声常走 Random Container（±几个半音、±几 dB）。
   → 用**频谱包络/倒谱类特征**（对音高小漂移不敏感）而不是精细谱线；或对样本做 ±3% 变速增强。
4. **你的装备会改变听感（ABI 专属，很重要）**：
   - **防暴头盔** = 全局降音量 + 加低通（声音变闷）；
   - **战术耳机** = 改变关键音效的**最大衰减距离**（听得更远）、降低环境声。
   （手游版《暗区突围》的音频模块分析已确认这套机制：见 [OFF 的游戏音频空间 · 暗区突围音频模块复现](https://dvatioff.space/2022/09/27/arena-breakout-reproduction/)）
   → **"装备档案（Profile)"机制**：`不戴 / 头盔 / 耳机` 三套档案，各自拥有独立的模板与阈值；
     采集/打标/识别都在当前档案下进行，一键切换。
   → 或者统一在"最坏情况（戴头盔 = 低通）"下建库，匹配时也套同一档案。

### 6.3 识别档位
- **L1 模板匹配**（先做）：多样本取最高分 + 阈值 + top1/top2 margin + 冷却去抖。
- **L2 小 CNN**（L1 对 3D 音不够时上）：用**增强数据**训练（低通 300–3 kHz 模拟距离/遮挡、混响、加噪、±3% 变速、±6 dB 增益、混入 BGM），
  Python 离线训练 → 导出权重 bin → **Go 手写前向推理**（不引 onnxruntime，它要 cgo + DLL）。

### 6.4 不做的两件事
- 不做"声源方位识别"（要立体声相位/HRTF 分析，工作量翻倍，收益不确定）。
- 不做"一句话概括战术局势"的花活，先把"这是什么声音"做准。

---

## 7. 悬浮窗规格（原生 Win32）

窗口样式：
```
WS_POPUP
+ WS_EX_LAYERED       逐像素透明
+ WS_EX_TRANSPARENT   鼠标穿透（绝不挡操作，ABI 里这条是刚需）
+ WS_EX_TOPMOST       置顶
+ WS_EX_NOACTIVATE    不抢焦点（不让游戏失焦）
+ WS_EX_TOOLWINDOW    不出现在任务栏/Alt-Tab
```
绘制：`CreateDIBSection` 32bpp（BGRA 预乘 alpha）→ 画图标 + 文字 + 置信度环 → `UpdateLayeredWindow`。
- 图标：库里统一 128×128 PNG，显示尺寸可调（64–192），用 `golang.org/x/image/draw` 的 CatmullRom 缩放并缓存。
- 文字：GDI `TextOutW` 到内存 DC（"白色 + 1px 黑描边"，任何背景都看得清）。
- 动画：淡入 60 ms → 保持 900 ms → 淡出 250 ms；同时最多叠 3 个（横向排布）。
- 位置：**默认右下角**（距离工作区右边/下边各 24 px，图标 96 px、不透明度 85%），但提供**显式坐标配置**——设置页里直接填 `X / Y`（物理像素）+ 选显示器，实时预览；也提供九宫格锚点作为快捷方式。
  坐标全部按 `PerMonitorV2` DPI 换算，换显示器/改缩放后仍落在你填的位置。
- 热键：`F9` 显示/隐藏悬浮窗（应急开关）、`F8` 键标录入。
- **ABI 注意**：请用**无边框窗口（Borderless）**模式玩，独占全屏下置顶窗口会被游戏盖住或闪烁；另外别把悬浮窗放在准星区域（挡视线 + 观感问题）。

---

## 8. 性能与体积预算

| 指标 | 预算 |
|---|---|
| 采集块 | 20 ms（960 帧 @48k 单声道） |
| 特征（1024 点 FFT ×4 + 64 mel + log） | ≈ 2–3 ms（纯 Go） |
| 量化 + 全库检索（1600 条 × 128 维 int8） | < 0.1 ms |
| 端到端（声音 → 图标出现） | **≈ 60–100 ms** |
| CPU | 单核 2–5%（空闲时按 VAD 跳过计算） |
| 内存 | 常驻 20–40 MB（含库与图标缓存） |
| exe | **3–4 MB**（`CGO_ENABLED=0 -trimpath -ldflags "-s -w"`） |
| 库文件 | 每个音效 ≈ 5–20 KB（图标 + 1 秒 wav + 向量）→ 100 个音效 ≈ 1–2 MB |

---

## 9. 暗区突围：无限的音效素材从哪来

1. **游戏本体拆包**（效率最高）：ABI 是网易的 UE 游戏，音频层大概率是 **Wwise**
   （同 IP 的手游版音频分析已确认使用 Wwise+衰减总线机制）。
   路径：`pak`（可能 AES 加密，密钥需社区渠道）→ 用 FModel / CUE4Parse 浏览 →
   导出 `.bnk` / `.wem` → 用 [Willie169/bnkextr](https://github.com/Willie169/bnkextr) 解 bnk、
   用 [scolopendre/wem2wav](https://github.com/scolopendre/wem2wav) 或 vgmstream 转 wav。
   **注意**：拆出来的是"干音"，没经过游戏的 3D 衰减/遮挡/混响/装备滤波 →
   只能当**种子样本**，必须补空间化增强（第 6.2 节），否则实战匹配率会很难看。
2. **游戏内录制 + 键标**（最可靠）：这是唯一能拿到"和你实际听到的一模一样"的样本的途径，
   **主力方式**，尤其对世界 3D 音。
3. **外部素材**：Wiki / B站/YouTube 音效合集，当参考库。

> 合法性：拆包仅限**个人研究**，不要分发提取出的版权音频；竞技游戏的自动化辅助工具请自行评估规则风险。

---

## 10. 安全红线（ABI 用 ACE 反作弊）

ABI 使用 **ACE（Anti-Cheat Expert）**，且游戏有"检测到异常"机制（[相关讨论](https://ghostingsquad.com/threads/4705/#post-6355)）。因此明确划死：

**只做**：读系统音频输出（WASAPI loopback）、画自己的窗口、写自己的日志与库文件、用系统官方 `RegisterHotKey`。

**绝不做**：
- ❌ 注入 DLL / hook 游戏 / 读写游戏内存 / 改游戏文件
- ❌ 模拟键鼠输入（SendInput / 驱动级）
- ❌ 底层键鼠钩子（`SetWindowsHookEx` 全局钩子）——很多反作弊对它高度敏感，`RegisterHotKey` 足够
- ❌ 读取游戏画面做识别（本项目不做，也不建议）

**建议的验证顺序**：先只开采集（不显示悬浮窗）打一局训练场 → 再开悬浮窗 → 确认 ACE 无异常提示后再正常使用。
如果 ACE 对置顶窗口有反应，降级方案：把提示窗口固定放在**副屏**，或只用**日志 + 声音提示**模式。

---

## 11. 路线图

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P0** | 纯 Go WASAPI loopback 采集 → WAV（`devices` / `capture` 子命令）+ 端到端验证 | ✅ 完成（880 Hz 端到端 PASS，负对照 FAIL，exe 3.4 MB） |
| **P1** | `library.srz` 库格式 + 管理端（列表/详情/上传音频/命名/配图标，ZIP 容器、原子写、Range 回放、MP3） | ✅ 完成（真实 HTTP 全流程 + 重启持久化复核通过） |
| **P2** | 自写 FFT + log-mel + int8 量化指纹库 + 扁平检索 + 匹配门控 + **实时打分面板/live CLI/SSE** | ✅ 完成（见下方实测数据） |
| **P3** | 原生悬浮窗（图标 + 名称 + 鼠标穿透 + 热键）+ 设置页（端点选择 / 悬浮窗坐标） | ✅ 完成（截图证据：图标真的出现在屏幕上；见下方"P3 的两个真 bug"） |
| **P4** | **热键回溯保存（F8）+ 候选项收件箱 + 管理端录入闭环** | ✅ 完成（采集→环形缓冲→落盘→`wavstat` 判定 880 Hz PASS；HTTP 提名闭环实测；F8 只验注册+内部触发，未合成按键） |
| **P5** | 装备档案（头盔/耳机）+ 空间化数据增强 + 离群样本清理 + L2 小 CNN / 库分享导出 | 待做 ← **下一步** |

### P3 的两个真 bug（都靠"截图取证"才抓出来）

1. **`SIZE` 被当成两个 16 位半字打包** → 悬浮窗永远不可见。
   `UpdateLayeredWindow` 的 `psize` 是 `SIZE`，即**两个 32 位 LONG** `{cx, cy}`。
   早期代码写的是 `size := uint32(uint32(h)<<16 | uint32(w))` 再取地址传进去，
   于是 ULW 把 `cx` 读成整个打包值（`108<<16|300 = 7078188`）、`cy` 读到栈上的垃圾，
   **把窗口改成了 700 万像素宽、0 像素高的图层**：ULW 返回成功、帧缓冲内容完全正确、
   `IsWindowVisible=true`、窗口样式全对，但屏幕上什么都看不到，而且每帧都把窗口矩形写坏
   （`R=7080424 B=1260`）。之前有人为这个现象打了个"每帧 SetWindowPos 复位矩形"的补丁——
   那是在给假象打补丁；改成真正的 `size` 结构体后补丁已删除，矩形也不再被写坏。
2. **重复预乘 alpha** → 整体偏暗（`image.RGBA` 本身就是预乘的，写 DIB 时只需要 RGBA→BGRA 重排）。
3. 顺带修：同一音效连续命中会占满多个格子（现在改为**刷新那一个格子**）；
   长名字溢出到相邻格子（现在按格子宽度截断并加「…」）。

> **教训（写进流程）**：验证 GUI 只能认"屏幕截图里真的有像素"。
> 返回码成功、`IsWindowVisible`、`GetWindowRect`、自己 dump 的帧缓冲**都不算证据**——
> 这个 bug 就是在这些指标全绿的情况下藏了一整轮。
> 判据补充：先用一个普通置顶窗口 + 一个 `SetLayeredWindowAttributes` 分层窗口做对照，
> 证明"本会话 GUI 可见"，再去怀疑自己的 `UpdateLayeredWindow` 路径。

### P2 实测数据（真实设备，非模拟）

| 指标 | 实测 | 备注 |
|---|---|---|
| FFT vs 朴素 DFT 最大误差 | 6.2e-11 | 要求 < 1e-9 |
| int8 量化 SNR | 42.55 dB | 要求 > 30 |
| `Search`（1600 模板 × 2048 维） | **2.26 ms** | 目标 < 3 ms |
| 流式分块一致性 | 逐元素差 **0.000e+00** | 非对齐分块 vs 一次性 |
| 真实匹配分数 | **0.995 ~ 1.0000** | 4 个合成音效自匹配 |
| 非匹配分数 | **-0.06 ~ -0.04** | 未注册声音 / 错库 |
| 单块处理耗时 | 0.045 ~ 0.104 ms/20 ms 音频 | 预算 10 ms，用掉约 1% |
| CPU 占用 | 0.16%（16 核口径） | 采集 + 识别 |
| 内存 | 堆 2.6–3.1 MiB；峰值工作集 21.3 MiB | 长跑有界（30021 块 = 10 分钟，HeapInuse 不增长） |
| 端到端延迟 | 采集块 10 ms + 窗长 187 ms | 命中在 0.17–0.79 s 位置正确 |

> 关键设计决定：`log-mel` 地板 = **-20 dB**（不是教科书的 -100 dB）。取 -100 dB 时未注册声音会拿到 0.8157（几乎无区分度），取 -20 dB 后降到 0.2345、带噪识别从 0.549 升到 0.892。该值已进特征指纹，改动即不兼容（索引会自动重建）。

**P1 结束你就能用**：上传音频、起名、配图标、看到库；**P3 结束就能实战**：听到声音→悬浮窗出图标。

---

## 12. 已确认的决定（2026-02 需求澄清）

| # | 决定 | 影响 |
|---|---|---|
| 1 | **图标由你自己准备** | 不做内置图标集。仍保留"批量导入时自动生成占位图（名称首字母 + 哈希配色）"，事后逐个替换 |
| 2 | **悬浮窗在右下角，且支持按坐标调整** | 默认右下角 24 px 边距、96 px 图标、85% 不透明度；设置页可填 X/Y 物理像素 + 选显示器，九宫格锚点作为快捷方式；全部走 DPI 换算（见第 7 节） |
| 3 | **管理端直接上独立窗口** | 用纯 Go 的 WebView2 绑定做窗口壳（无 cgo），页面/服务复用同一套；WebView2 运行时缺失时自动退回默认浏览器（见第 2 节）；正在做技术验证 spike |
| 4 | **采集走哪路必须可选** | 设置页提供渲染端点下拉（由 `devices` 枚举填充），支持热切换、持久化到配置；设备消失/被拔掉时自动回退到默认端点并提示 |

> 本机实测只有 **1 个活动渲染端点**：`扬声器 (Realtek(R) Audio)`（`DEFAULT, DEFAULT-COMMS`）。
> 所以"可选"主要是为将来接 HDMI/显示器/虚拟声卡准备的，默认路径仍然是它。

---

## 13. 环境阻塞：本机 Smart App Control 会拦我们编译的 exe（必须处理）

**这不是游戏/ACE 的问题，是 Windows 自己在拦我们自己刚编译出来的程序。**

实测证据（本机，2026-09）：
- `HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy\VerifiedAndReputablePolicyState = 1` → **Smart App Control 处于强制模式**。
- 事件日志 `Microsoft-Windows-CodeIntegrity/Operational`：`Event 3118 Smart App Control Block` + `Event 3077`
  `attempted to load ...\soundradar.exe that did not meet the Enterprise signing level requirements or violated code integrity policy (Policy ID:{0283ac0f-fff1-49ae-ada1-8a933130cad6})`。
- PowerShell 直接报：`Program 'soundradar.exe' failed to run: An Application Control policy has blocked this file`。

**判定方式是"按文件哈希"，不是按路径、也不是按体积**：
| 文件 | 体积 | 结果 |
|---|---|---|
| `soundradar.exe`（默认 build） | 11,957,248 B | ❌ 连续 5/5 被拦 |
| 同文件改名副本 | 11,957,248 B | ❌ 仍被拦（证明与路径无关） |
| **`soundradar_slim.exe`（`-s -w` 重编）** | 8,129,024 B | ✅ 正常启动，`devices` 输出正确 |
| `wavstat.exe` | 2,984,448 B | ✅ 正常 |
| `spike_webview2.exe` | 10,542,592 B | ✅ 正常（比被拦的那个还大 → 与体积无关） |

**影响**：任何一次重新编译都可能撞上被拦的哈希；对一个"打游戏前启动一下"的小工具来说这是硬伤。
（SAC 没有排除项/白名单机制。）

**可选对策**：
1. **关闭 Smart App Control**：设置 → 隐私和安全性 → Windows 安全中心 → 应用和浏览器控制 → 智能应用控制 → 关闭。
   ⚠️ **单向操作**：关掉后想再开启必须**重装/重置 Windows**（微软官方行为）。Defender 杀毒和 SmartScreen 不受影响。
2. **构建后自检 + 换哈希重试**（✅ **已实施**，不改动系统安全设置）：`soundradar\build.ps1` 现在会
   编译 → 用零副作用的 `version` 子命令真启动一次 → 若被拦就换一个随机 build salt
   （`-ldflags "-s -w -X main.buildSalt=<random>"`）重新编译再试，默认最多 6 次，全失败才报错并提示上面的选项 1。
   用法：`Set-ExecutionPolicy -Scope Process Bypass -Force; .\build.ps1 -Test`
   产物固定输出到 `soundradar\bin\{soundradar.exe, wavstat.exe}`，脚本最后会打印每次尝试的 salt/体积/判定 + sha256 + 版本自检输出。
   实测：负对照（拿已知被拦的 exe 喂给自检逻辑）能正确判定 `BLOCKED`；随后一次完整构建 **第 1 次尝试就放行**
   （`bin\soundradar.exe` 8,130,560 B，`go vet` / `go test ./...` 全绿）。
   ⚠️ 诚实说明：**"被拦后自动换哈希重试"这条分支本身没有被真实触发过**（我们造不出一个必然被拦的哈希），
   被验证的是"检测逻辑正确" + "正常路径可用"。
3. **代码签名证书**（OV/EV）：能根治，但要花钱，自签名证书不满足 SAC 要求。
4. 保持现状：被拦了手工重编一次。**不可靠，不推荐**。
