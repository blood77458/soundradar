# 录音与打标指南（暗区突围：无限）

> 这份指南解决项目里**唯一无法由代码完成**的部分：建立"声音 ↔ 含义"的对应关系。
> 目标：每类关键音效拿到 5 条干净样本，覆盖它真实的听感变化。

---

## 0. 为什么必须你自己录

游戏里的 3D 音效**每次播放都不一样**，因为叠了四层处理：

| 处理 | 后果 |
|---|---|
| 距离衰减 | 远了先掉高频，频谱形状变了 |
| 遮挡（隔墙/掩体） | 叠加低通，声音变闷 |
| 室内外混响 | 尾巴变长 |
| Wwise 随机音高/音量 | 同一声源每次略有差异 |
| **你的装备** | **防暴头盔 = 全局低通 + 降音量；战术耳机 = 改变最大衰减距离** |

所以从游戏 pak 里拆出来的"干音"只能当种子，**真正能用的模板必须来自你在游戏里实际听到的录音**。

---

## 1. 录什么：先只做这 8 类

不要贪多。覆盖这 8 类，收益已经非常大（也便于先验证准确率）：

| 优先级 | 类别 | 为什么重要 |
|---|---|---|
| ★★★ | 脚步（他人） | 判断有人靠近，最需要 |
| ★★★ | 换弹 | 判断敌人在补给/暴露时机 |
| ★★★ | 开枪（他人） | 定位交火 |
| ★★☆ | 开门 / 关门 | 判断有人进屋 |
| ★★☆ | 玻璃破碎 | 高频、清晰、好识别 |
| ★★☆ | 手雷（拉环 / 落地 / 爆炸） | 保命信息 |
| ★★☆ | 搜包 / 拾取 | 判断敌人正在做什么 |
| ★☆☆ | 受伤 / 受击 | 自身状态 |

---

## 2. 录音前必须做的事（否则白录）

1. **关掉所有音频后处理**：
   - 游戏内：夜间模式 / 音量均衡 / 动态范围压缩 → 全部关闭；
   - Windows：设置 → 系统 → 声音 → 增强音频 / 空间音效 → **关**；
   - Realtek 音频控制台：均衡器、响度均衡 → **关**；
   - 关闭杜比 / DTS / 耳机虚拟环绕。
2. **音量别动**：录完一批之前不要改系统音量或游戏音量（改了就重录）。识别端会做响度归一化，但变动太大会影响阈值。
3. **用有线耳机或扬声器**（蓝牙耳机有 100–300 ms 延迟，不影响的识别准确率，但录的时候容易和屏幕事件对不上）。
4. **确认录的是哪一路**：`bin\soundradar.exe devices` 看端点，用 `--device <名字子串>` 指定。

---

## 3. 怎么录：一条命令一次

```powershell
cd C:\Users\znz_l\PycharmProjects\sound_check\soundradar

# 录 10 秒到一个带含义的文件名
.\bin\soundradar.exe capture --seconds 10 --out "D:\sr\脚步-近-无遮挡-耳机-01.wav"
```

**录制技巧（关键）**：不要指望录一整局再切。
**一次只针对一个事件**，触发它，然后马上停止录音。比如：

- 想让队友在近处跑 → 让他跑，你录 8 秒，事后在管理端里切出那一小段；
- 想录换弹 → 连续换弹 3 次，一次录 10 秒，里面就有 3 个候选；
- 想录开门 → 反复开关同一扇门，一次录 15 秒。

**用游戏里的可见后果来确认时刻**（听不出来没关系）：
"我按了 R 之后出现的那声" = 换弹音；"弹夹数字变了" = 换弹完成；
"屏幕红了" = 受击音；"队友从门口跑过去" = 近处脚步。

---

## 4. 命名规范（重要，别省）

```
<类别>-<距离>-<遮挡>-<装备>-<序号>.wav
```
示例：
```
脚步-近-无遮挡-耳机-01.wav
脚步-远-无遮挡-耳机-01.wav
脚步-近-隔墙-耳机-01.wav
换弹-近-无遮挡-无头盔-01.wav
换弹-近-无遮挡-头盔-01.wav
开门-近-无遮挡-耳机-01.wav
```

为什么这么细：**距离和遮挡会让同一个音效的特征完全不同**，而装备（头盔低通）会整体改变听感。
将来在库里的做法是"一个条目 + 多个变体样本"，文件名就是你的证据链——出问题时能一眼看出是哪个变体在误报。

**每类至少 5 条**，且刻意覆盖：
- 近（3–5 m）/ 中（10–15 m）/ 远（20 m+）
- 无遮挡 / 隔一堵墙
- 戴不戴头盔（能测就测，测不了至少标注一下当时装备）

---

## 5. 录完怎么筛（P2 完成后）

```powershell
# 把某段录音和已注册的库比对，看它到底最像哪个
.\bin\soundradar.exe match --wav "D:\sr\脚步-近-无遮挡-耳机-01.wav" --top 5
```
输出会给出排名和分数：
- **候选段分数 > 0.9** → 干净样本，直接注册；
- **0.75–0.9** → 可用，但建议和别的变体一起注册；
- **< 0.75 或第一名是别的类别** → 这段里混了别的声音（BGM/枪声/语音），重录或裁短。

管理端（`bin\soundradar.exe serve` → `http://127.0.0.1:8765`）里有**实时打分面板**（P2 后半段加上），
可以把游戏开着，看哪根柱子飙起来，直接把当前声音注册成条目——这是最省事的打标方式。

---

## 6. 常见坑

| 坑 | 表现 | 对策 |
|---|---|---|
| BGM 混进来 | 每次都匹配到同一类 | 录的时候尽量在安静环境/无音乐场景；管理端里裁短 |
| 队友语音混进来 | 分数普遍偏低 | 同上；后续可考虑只抓游戏进程音频（Process Loopback） |
| 录到的是静音 | 匹配分数为 0 / 全是静音 | 用 `wavstat analyze` 看 peak dBFS，低于 -60 就是没录上 |
| 音量中途被改 | 阈值失配 | 一批录完前别动音量 |
| 同一类里混了不同距离 | 准确率忽高忽低 | 按命名规范分开录，注册成同一 item 的多个样本 |
| 一次录太长 | 一个文件里挤了十几个事件 | 一次只针对一个事件，8–15 秒足够 |

---

## 7. 进展记录表（建议自己维护一份）

| 日期 | 类别 | 文件名 | 距离 | 遮挡 | 装备 | 条数 | 备注 |
|---|---|---|---|---|---|---|---|
| | 脚步 | 脚步-近-无遮挡-耳机-01..05 | 近 | 无 | 耳机 | 5 | 训练场 |
| | 换弹 | | | | | | |
| | 开枪 | | | | | | |
| | 开门 | | | | | | |
| | 玻璃 | | | | | | |
| | 手雷 | | | | | | |
| | 搜包 | | | | | | |
| | 受击 | | | | | | |

---

## 8. 保存位置建议

```
D:\sr\
  原始录音\        ← capture 直接写这里，不删
  已入库\          ← 裁剪好、确认干净的样本
  library.srz      ← 库文件（管理端生成，记得备份）
  index.bin        ← 指纹索引（重算即可，不用备份）
```

`library.srz` 是 ZIP，随时能用解压软件打开检查；**这个文件建议定期备份**，里面是你所有手工劳动的成果。
