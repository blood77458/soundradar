# 本次改动说明（0.6.0-p4 → 0.7.0-p5）

本文记录这次会话对 SoundRadar 做的全部改动、与改动前的区别，以及迁移到新电脑时需要注意的事项。

- 起始版本：`0.6.0-p4`
- 当前版本：`0.7.0-p5`
- 改动范围：新增托盘与双击启动、新增环境音过滤、加固采集链路、补充诊断与文档

---

## 一、需求与结论对照

| 需求 | 状态 | 实现位置 |
|---|---|---|
| 双击 exe 直接启动 | ✅ | `cmd/soundradar/main.go`（无参数即走托盘启动器） |
| 命令行启动方式保留 | ✅ | 所有原有子命令未改名、未改参数 |
| 系统托盘图标，可呼出管理端与退出 | ✅ | `internal/tray/`（纯 Win32，无第三方依赖） |
| 游戏内有杂音/环境音时提高识别率 | ✅ | `internal/dsp/noise.go` + `internal/index`（锚点） |
| 库/索引一致性 | ✅ | 噪声参数进入指纹；索引自动重建；指纹重算不再因加字段漂移 |
| 无法使用的场景要提示该做什么 | ✅ | `internal/capture` 的失败报告 + 建议清单 |

---

## 二、双击启动 + 系统托盘（新增）

### 行为

双击 `soundradar.exe`（不带任何参数）现在会：

1. 在系统托盘放一个雷达图标；
2. 启动 `serve --overlay`：管理页面 + 实时识别 + 命中悬浮窗；
3. 弹出气泡提示，告知管理界面地址。

托盘菜单：

| 菜单项 | 作用 |
|---|---|
| 打开管理界面 | 在浏览器打开 `http://127.0.0.1:8765/` |
| 查看实时识别状态 | 同上（管理端「实时打分」页） |
| 显示/隐藏命中悬浮窗 | 提示悬浮窗快捷键（默认 F9） |
| 打开候选项目录 | F8 存下来的片段所在目录 |
| 打开程序数据目录 | `config.json` / `data\` 所在目录 |
| 退出 soundradar | 停掉识别、悬浮窗、托盘图标 |

左键单击或双击托盘图标 = 打开管理界面。

### 与改动前的区别

- 改动前：直接双击 exe 会因为"缺少子命令"打印用法并以退出码 2 结束；必须手打
  `soundradar serve --overlay --open`。
- 现在：双击即用，且进程有可见入口和退出口。
- 命令行行为**完全不变**：任何带子命令的调用走原来的分支（包括 `version`，`build.ps1` 的启动自检不受影响）。

### 关键实现点

- **单实例**：启动前先探测端口，已在监听则直接复用并打开页面，不会起第二个服务、不会出现第二个托盘图标。
- **托盘图标只有一个**：被启动的 serve 子进程不带 `--tray`，图标统一由启动器负责（否则会出现两个图标、两个"退出"入口）。
- **子进程随启动器退出**：serve 子进程被放进 Windows 作业对象（job object），启动器被任务管理器强杀时子进程也会结束，不会留下没有图标的采音进程。
- **图标是代码生成的**：`tools/iconmake` 画出雷达图标并写出 `.ico` 与内嵌 Go 数据，仓库里不需要二进制图片文件。
- **隐藏窗口**用普通顶层窗口（不是 message-only），这样 Explorer 重启后能收到 `TaskbarCreated` 广播并重新注册图标。

### 新增文件

```
internal/tray/tray.go              跨平台部分（API、菜单、.ico 解析）
internal/tray/tray_windows.go      Win32 实现（Shell_NotifyIconW + 菜单 + 消息循环）
internal/tray/tray_other.go        非 Windows 桩
internal/tray/tray_test.go         菜单与 .ico 解析测试
cmd/soundradar/tray_cmd.go         `soundradar tray` 启动器（含子进程管理）
cmd/soundradar/tray_cmd_test.go    子进程参数回归测试（禁止带 --tray）
cmd/soundradar/serve_tray.go       `serve --tray` 的托盘接线
cmd/soundradar/launch_windows.go   控制台探测 / exe 路径
cmd/soundradar/launch_other.go     非 Windows 桩
cmd/soundradar/job_windows.go      作业对象（子进程跟随退出）
cmd/soundradar/job_other.go        非 Windows 桩
cmd/soundradar/icon_data.go        内嵌图标（由 tools/iconmake 生成）
cmd/soundradar/icon_data_test.go   内嵌图标格式测试
tools/iconmake/main.go             图标生成器
```

### 新增命令与参数

```powershell
soundradar.exe                       # 双击等价：托盘 + 管理端 + 悬浮窗
soundradar.exe tray [--port N] [--no-open] [--no-serve]
soundradar.exe serve --tray          # 命令行方式也带托盘
```

`serve` 的 `--tray` 是新增参数，原有参数（`--port` / `--open` / `--overlay` / `--library` / `--config` / `--strict-port` / `--quiet`）都保留。

---

## 三、环境音过滤（新增，识别率的主要提升）

### 调试过程中的关键发现

一开始的假设是"环境音抬高了安静频段、拉低余弦相似度"，于是按部就班做了逐频点噪声抑制。但实测发现**真正吃掉识别率的不是逐频点噪声，而是锚点不稳定**：

- 用特征时取"最响的那一帧"作为锚点，而一个几十毫秒的音效中间有好几帧能量几乎相同；
- 环境音会让"最响的一帧"在几次噪声实现之间跳动最多 **±5 帧（约 27 ms）**；
- 结果模板和现场窗口描述的是**不同的物理帧**，余弦相似度直接崩掉（实测 1.00 → 0.47）。

修掉锚点之后，强环境音下的结果：

| 指标 | 改动前 | 改动后 |
|---|---|---|
| 同一段声音与自身模板的相似度 | 0.63 | **0.84** |
| 与干扰音的差距（margin） | +0.14 | **+0.31** |
| 干净录音的自相似度 | 1.0000 | **0.9998**（不受影响） |

### 现在的四层处理（按信号流向）

1. **高通**（默认 120 Hz，2 阶 Butterworth）：去掉隆隆声、风噪、直流。这些能量本来就落在最低 Mel 频段之外。
2. **逐频点 Wiener 增益抑制**：用最近约 256 ms 的**最小统计量**估计每个频点的稳态噪声功率，再按 `g = P / (P + α·N)` 压制。
   - **偏差修正**（关键）：N 帧最小值的期望只有均值的 `1/(N²+N)`（N=24 时约 600 倍）。不修正的话增益恒等于 1，整个滤波器等于没做——这是实现过程中实测踩到并修掉的问题。
   - 增益下限（默认 −14 dB）保证单个频点不会被抹掉。
3. **自适应静音门限**：门限跟着实测噪声底走（实测校准误差 0.22 dB）。带两个保护：
   - 只升不降：噪声底低于配置门限时保持配置值（默认 −60 dBFS）；
   - **动态范围守卫**：只有看到"峰值明显高于噪声底（≥12 dB）"时才认为测到的是真静音。否则（持续音、音乐床）会把门限顶到信号上、把自己彻底屏蔽——这是实测发现的失败模式。
4. **平滑后的锚点**（`internal/index`）：帧能量先做 15 帧中心移动平均再取峰值。这是本次提升最大的一处。

### 与改动前的区别

- 特征算法名从 `mel-goertzel-v1` 变为 `mel-goertzel-v2`（版本 1 → 2），因为每个向量都变了。
- **首次启动会重建 `data\index.bin`**：程序发现指纹不匹配时会用 `data\library.srz` 里保存的原始音频重算索引。库文件本身不受影响。
- 干净录音几乎不受影响（自相似度 0.9998），所以原有声纹库不需要重录。

### 配置项（`config.json` 新增 `noise` 段）

```json
"noise": {
  "method": "subtract",   // subtract = 高通 + 逐频点抑制；highpass = 只做高通；off = 全关
  "highPassHz": 120,
  "strength": 2.0,        // 过减系数：越大压得越狠
  "gainFloorDb": -14,     // 单个频点最多被压掉多少 dB
  "adaptiveGate": true,   // 自适应静音门限
  "gateMarginDb": 6,      // 门限比噪声底高多少 dB
  "gateFloorDbfs": -70    // 自适应门限的下限
}
```

这些参数属于**指纹**的一部分：改动后下次启动会自动重建索引。老的 `config.json`（没有 `noise` 段）会按默认值工作，不需要手工补。

### 新增/修改文件

```
internal/dsp/noise.go        环境音前端（高通、噪声跟踪、增益、门限）+ 测试
internal/dsp/noise_test.go   8 个测试：提升、干净不退化、电平标定、门限行为、
                             分块一致性、不改调用方缓冲、高通、参数校验、指纹覆盖
internal/dsp/dsp.go          Params.Noise 字段、指纹文档、Analyzer 接线
internal/index/index.go      平滑锚点（anchorSmoothFrames）+ 索引指纹改由 dsp.ParamDoc 重算
internal/config/config.go    NoiseConfig + 归一化 + 校验
```

### 实现过程中修掉的两个真 bug

- `Push` 会就地修改调用方的音频缓冲 → 同一段音频被过滤两次（已有 `TestNoiseDoesNotMutateInput` 锁死）。
- 分块推送与一次性推送结果不一致 → 改成按**绝对采样位置**锚定估计窗口（`TestNoiseChunkIndependence` 对三种模式验证）。

---

## 四、采集链路加固（针对 `IAudioClient::Initialize` 报错）

### 问题

有机器报告：

```
IAudioClient::Initialize(shared/loopback) failed: error 2290679816
(FormatMessage failed with: The system cannot find message text ...)
```

`2290679816 = 0x88890008 = AUDCLNT_E_UNSUPPORTED_FORMAT`。后半句是误导：Windows 消息表里没有 `AUDCLNT_*` 这族 HRESULT，所以 `FormatMessage` 查不到文案。它的真实含义是：**这个播放端点拒绝了它自己刚报出来的混合格式**（音频增强、空间音效、独占占用、蓝牙免提档等）。

### 现在的行为（自动化优先级从高到低）

1. **换格式**：端点拒绝自己的混合格式时，依次再试 48 kHz 立体声（浮点/16 位）、44.1 kHz、48 kHz 单声道、16 kHz 单声道、端点自身采样率。识别链路的两级重采样会自动适配任何采样率。
2. **换端点**：按"指定的/默认 → 另一个角色的默认（eConsole/eMultimedia/通讯默认）→ 其余端点"顺序试，第一个能开的就用。
3. **放宽格式支持**：8–32 位整数（含 20/28 位这类非字节对齐的打包格式）与 32/64 位浮点全部可转换，不再因为"位数奇怪"直接放弃。
4. **全部失败时打印可操作报告**：端点清单 + 每个失败原因 + 按概率排序的处理清单（关音频增强 → 关空间音效 → 蓝牙选立体声 → 关独占程序 → 查 Windows Audio 服务 → 更新驱动 → VB-Cable 兜底）。
5. **录音成功但全是静音时**也提示（命令行 `capture` 录完整段全 0 时）。

### 与改动前的区别

- 改动前：`Initialize` 失败就直接报错退出，错误文本是一串看不懂的数字；`--device` 指定了打不开的端点也不会自动回退。
- 现在：能自动就自动，不能自动就给出"该去关哪个开关"的清单。
- **实时识别不弹静音提示**（按你的要求）：连续 20 秒听不到声音不会打断你，看终端的电平条或管理端「实时打分」页的电平条即可。静音相关的提示只保留在命令行 `capture` 录完之后。

### 新增命令

```powershell
soundradar.exe devices --probe    # 逐个端点试开一次，报告谁可用、各自的混合格式
```

输出示例：

```
OK    [0] 扬声器 (Realtek(R) Audio)
        混合格式: 48000 Hz / 2 ch / 32-bit float
FAIL  [1] 耳机 (Bluetooth Hands-Free)
        混合格式: 16000 Hz / 1 ch / 16-bit PCM
        打不开  : AUDCLNT_E_UNSUPPORTED_FORMAT (0x88890008)：…
```

### 新增/修改文件

```
internal/capture/session_windows.go        统一打开逻辑、端点回退、格式协商、错误翻译
internal/capture/format_windows.go         WAVEFORMATEXTENSIBLE 手工构造 + 候选格式表
internal/capture/format_windows_test.go    逐字节布局测试（错位会导致同一个报错且编译期不报）
internal/capture/probe_windows.go          Probe() API
internal/capture/health_windows.go         健康检查
internal/capture/session_windows_test.go   HRESULT 数值与文案测试
internal/capture/capturer.go               EndpointFailure、PrintTroubleshooting、
                                          PrintEndpointFailureReport（平台无关）
internal/capture/loopback_windows.go       改用 session；放宽格式支持；通用位数转换
internal/capture/stream_windows.go         实时链路共用同一套打开逻辑
cmd/soundradar/audio_health.go             各命令共用的失败报告与"注意"输出
cmd/soundradar/main.go                     devices --probe；capture 失败/静音提示
```

---

## 五、其他修复与改动

- **索引指纹不再漂移**：`internal/index` 原来自己复刻一份参数 JSON 结构，加字段就会让所有已存索引读不出来。现在改为复用 `dsp.ParamDoc` 并由 dsp 重算指纹，新增字段会自动带上。
- **`live`/`overlay` 的噪声参数一致**：两个命令会读同一份 `config.json` 的 `noise` 段；`match` 只在显式给 `--config` 时才改变管线（避免悄悄重建正在被 serve 使用的索引）。
- **`--device` 写错时不再打印长清单**，只提示正确写法（会从设备名里自动提取可用片段）。
- **`.gitignore`** 增加 `soundradar/cmd/soundradar/config.json` 与 `*.syso`。
- 版本号：`0.6.0-p4` → `0.7.0-p5`；用法文本、README、使用手册同步更新。

---

## 六、迁移注意事项

### 必须一起带走的文件

| 文件 | 说明 |
|---|---|
| `soundradar/data/library.srz` | 声纹库（你的数据，必须备份） |
| `soundradar/config.json` | 设置（exe 旁边，首次运行会自动生成） |
| `soundradar/data/candidates/` | F8 存下来的候选项（如果已经处理过可丢弃） |
| `soundradar/bin/soundradar.exe` | 本次会话新编译的 0.7.0-p5 |

### 首次运行会发生什么

- 因为特征算法从 v1 变成 v2，**第一次启动会自动用 `library.srz` 里的原始音频重建 `data\index.bin`**。库文件不受影响，只是首次启动多等一小会儿（自带库约 200 个模板，实测不到 1 秒）。
- 如果目标机器的 `config.json` 是旧的（没有 `noise` 段），会按默认值工作，不需要手工补。

### 编译环境

压缩包里**没有**打入编译环境：

- `.gopath/`（Go 依赖模块缓存，约几十 MB）
- `.gocache/`（Go 构建缓存，约几百 MB）
- `bin/` 下的编译产物（`soundradar.exe` / `wavstat.exe`）

在新电脑上重新编译：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
cd soundradar
.\build.ps1
```

前置条件：

- Windows + Go 1.27 或更高；**不需要 gcc**（构建脚本强制 `CGO_ENABLED=0`）。
- 首次编译会从 `goproxy.cn` 下载依赖（`go.mod` 里共 5 个直接依赖）。
- `build.ps1` 会把 GOPATH / GOCACHE 重定向到仓库内的 `.gopath` / `.gocache`，并在编译后做"真启动自检"。如果目标机器开了 Smart App Control，脚本会自动换构建盐重编，直到 exe 能启动（本次会话实测 1 次通过）。

### 建议的验收步骤（新电脑上）

```powershell
# 1. 看版本与构建信息
.\bin\soundradar.exe version

# 2. 看播放端点能不能打开（排查声音问题用）
.\bin\soundradar.exe devices --probe

# 3. 双击 bin\soundradar.exe —— 托盘出现雷达图标、浏览器打开管理页面

# 4. 管理端 →「实时打分」→ 开始，放一段音乐，确认电平条在动
```

---

## 七、验证记录

本次改动在本机（Windows，Go 1.27.1，CGO 关闭）的验证结果：

| 项目 | 结果 |
|---|---|
| `go vet ./...` | 通过，无输出 |
| `go test ./...` | 全部包通过（含新增的 dsp/tray/capture/cmd 测试） |
| `build.ps1` | 编译成功，Smart App Control 启动自检 `LAUNCHES OK` |
| 真实采集 | `capture --seconds 2` 走通，混合格式 48000 Hz / 2 ch / 32-bit float |
| 端点探测 | `devices --probe` 走通 |
| 双击启动 | 托盘图标创建成功，serve 子进程端口 8765 返回 200，且只有启动器持有托盘图标 |
| 强杀启动器 | serve 子进程被作业对象一并结束（剩余进程 0） |

已知的既有问题（与本次改动无关）：`internal/index` 的 `TestSearchPerformance` 在本机是临界项（约 3.1 ms 对 3 ms 目标），改动前后都会偶发失败。

---

## 八、文件清单（本次会话新增/修改）

### 新增

```
internal/tray/{tray.go,tray_windows.go,tray_other.go,tray_test.go}
internal/dsp/noise.go
internal/dsp/noise_test.go
internal/capture/{session_windows.go,format_windows.go,format_windows_test.go,
                  probe_windows.go,health_windows.go,session_windows_test.go}
cmd/soundradar/{tray_cmd.go,tray_cmd_test.go,serve_tray.go,audio_health.go,
                launch_windows.go,launch_other.go,job_windows.go,job_other.go,
                icon_data.go,icon_data_test.go}
tools/iconmake/main.go
本次改动说明.md
```

### 修改

```
go.mod / go.sum                  （无新依赖；x/text 本来就是间接依赖）
README.md
使用手册.md
录音与打标指南.md
.gitignore
cmd/soundradar/main.go
cmd/soundradar/serve.go
cmd/soundradar/live_cmd.go
cmd/soundradar/overlay_cmd.go
cmd/soundradar/recall_cmd.go
cmd/soundradar/recall_wire.go
cmd/soundradar/match_cmd.go
internal/dsp/dsp.go
internal/dsp/dsp_test.go
internal/config/config.go
internal/index/index.go
internal/live/live.go
internal/live/source_capture.go
internal/match/match.go
internal/server/server.go
internal/server/live.go
internal/server/live_test.go
internal/capture/capturer.go
internal/capture/loopback_windows.go
internal/capture/stream_windows.go
internal/capture/loopback_other.go
```

---

## 九、一处需要你留意的事故说明

在实现"取消实时静音提示"这一步时，我用 PowerShell 做批量文本替换，把 `cmd/soundradar/live_cmd.go` 和 `cmd/soundradar/overlay_cmd.go` 的中文内容写坏了（`Get-Content -Raw | Set-Content -Encoding UTF8` 造成的编码往返损坏，同时带入了 BOM）。

处理结果：

- 代码逻辑没有丢失：两个文件已按原结构重写，`gofmt` / `go vet` / `go test` 全部通过，实际命令行为逐条验证过；
- **中文注释与提示语是我重写的**，含义与原文一致但措辞可能略有出入；
- 如果你手上有这两个文件的原始版本，可以对比一下文案；代码行为不需要重新验证。

教训：在这个仓库里改文件只用编辑工具，不用 PowerShell 的 `Get-Content` / `Set-Content` 管道。
