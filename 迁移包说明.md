# 迁移包说明（先看这个）

这是 SoundRadar `0.7.0-p5` 的迁移包。解压后按下面的步骤在新电脑上跑起来。

## 包里有什么

```
soundradar-migration\
  迁移包说明.md          ← 你正在看的这份
  本次改动说明.md          ← 这次会话的全部改动、与旧版的区别
  README.md              ← 项目主说明
  使用手册.md             ← 日常操作
  录音与打标指南.md         ← 录音与打标方法
  test_p1.ps1 / verify_p1.ps1   ← 早期验收脚本（仓库根目录的那两个）
  soundradar\             ← Go 模块
    go.mod / go.sum       ← 依赖清单（go mod tidy 不需要跑）
    build.ps1             ← 构建脚本（编译 + Smart App Control 启动自检）
    cmd\  internal\ tools\ ← 源码
    data\library.srz      ← 声纹库（11 条目 / 17 样本）
    data\index.bin        ← 已按 0.7.0 的新算法重建好的索引
    verify_p3.ps1 / verify_p4.ps1 / verify_live.ps1 / run_go_test.ps1
    tools\ui_smoke.js
```

## 包里**没有**什么（按你的要求，编译环境没打进来）

| 未打入 | 原因 |
|---|---|
| `.gopath\`（仓库根目录） | Go 依赖模块缓存，首次编译会自动下载 |
| `.gocache\`（仓库根目录） | Go 构建缓存，首次编译会自动生成 |
| `soundradar\bin\*.exe` | 编译产物；`build.ps1` 会重新编出来 |
| `.git\` | 没有版本库 |

所以这个包只有约 1.8 MiB。**第一次编译需要联网**（从 `goproxy.cn` 拉 5 个依赖）。

## 在新电脑上怎么跑起来

### 第 1 步：确认有 Go

```powershell
go version
```

需要 **Go 1.27 或更高**。没有就先去 https://go.dev/dl/ 装。**不需要 gcc / Visual Studio**（构建脚本强制 `CGO_ENABLED=0`）。

### 第 2 步：编译

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
cd soundradar-migration\soundradar
.\build.ps1
```

脚本会：

1. 把 GOPATH / GOMODCACHE / GOCACHE 重定向到**仓库内**的 `.gopath` / `.gocache`（不动系统环境变量）；
2. 从 `goproxy.cn` 下载依赖；
3. 编译出 `bin\soundradar.exe` 和 `bin\wavstat.exe`；
4. **真启动自检**：如果新电脑开了 Smart App Control，脚本会自动换构建盐重编，直到 exe 能真正启动为止（它会一直重试到 `MaxAttempts`，默认 6 次）。

想跳过自检只编译（快一点）：`.\build.ps1 -NoVerify`

### 第 3 步：直接双击用

双击 `soundradar\bin\soundradar.exe`（不带参数）：

- 托盘区出现雷达图标；
- 浏览器自动打开管理页面 `http://127.0.0.1:8765/`；
- 开始听系统正在播放的声音，命中时弹出悬浮窗。

托盘图标：左键/双击 = 打开管理界面，右键 = 菜单（含**退出 soundradar**）。

### 第 4 步（可选）：先自检一下声音

```powershell
cd soundradar
.\bin\soundradar.exe devices --probe
```

它会逐个播放端点试开一次，报告哪些能用、各自的混合格式。如果显示某个端点打不开，按启动时打印的清单处理（关音频增强 / 关空间音效 / 蓝牙选立体声等），详见 `本次改动说明.md` 第四节。

## 要注意的三件事

1. **数据位置**：`config.json` 和 `data\` 都放在 **exe 旁边**（也就是 `soundradar\bin\`）。
   - 第一次运行会在 `bin\` 里自动生成 `config.json`。
   - 本包里的声纹库在 `soundradar\data\library.srz`（源码仓库位置）。**第一次运行前建议把它连索引一起复制到 `soundradar\bin\data\`**，这样双击启动就能直接用自带的 11 个条目：

     ```powershell
     cd soundradar
     New-Item -ItemType Directory -Force -Path bin\data | Out-Null
     Copy-Item data\library.srz, data\index.bin bin\data\ -Force
     ```

   - 之后自己录的条目也会写进 `bin\data\library.srz`，换电脑时单独备份这个文件即可。

2. **索引已经是新版**：包里的 `data\index.bin` 已按 `mel-goertzel-v2` 重建（指纹 `0534eb18…`），和 `library.srz` 配套。除非你改了 `config.json` 里的 `noise` 段，否则首次启动不会重建。

3. **耳机名字**：如果新电脑的播放设备名和旧机不同，到管理端「设置」里选采集端点。填错了也不会失败——程序会自动换用能打开的端点并打印说明。

## 想复现旧版行为？

改 `config.json`：

```json
"noise": { "method": "off" }
```

识别管线就回到 0.6.0 的算法。注意这属于**指纹**的一部分，改完首次启动会重建索引。

## 出问题时

```powershell
.\bin\soundradar.exe version           # 版本 / 构建盐 / 是否纯 Go
.\bin\soundradar.exe devices --probe   # 端点能不能打开、混合格式
go vet ./...                           # 源码层面的检查
go test ./...                          # 全部单元测试
```

已知的既有问题：`internal/index` 的 `TestSearchPerformance` 在本机是临界项（约 3.1 ms 对 3 ms 目标），偶发失败，与本次改动无关。
