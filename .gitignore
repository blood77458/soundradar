# Go caches. build.ps1 recreates these from goproxy.cn.
.gocache/
.gopath/

# Abandoned WebView2 experiments and local verification dumps.
spike/
artifacts/
soundradar/artifacts/

# Binaries and this machine's runtime data.
# The executable is published as a GitHub Release, not committed.
# config.json names the local playback device and overlay position.
# F8 candidates are temporary inbox clips, not the published library.
soundradar/bin/
soundradar/data/candidates/
# config.json is generated next to the exe (or, during `go run`, next to the
# package): it names the local playback device and overlay position.
soundradar/cmd/soundradar/config.json
*.exe
*.bak
*.syso

# Local migration kit. It packs chat sessions and login credentials.
迁移打包.ps1
新电脑部署.md

# OS junk
Thumbs.db
Desktop.ini
