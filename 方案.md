# 游戏音效识别助手（Sound Radar）— 技术方案

> **实现语言选型：Go。** 具体见 [`方案-go实现.md`](方案-go实现.md)（纯 Go / 无 cgo / 单文件 exe / 量化向量库）。
> **需求已锁定后的实现规格：见 [`方案-落地设计.md`](方案-落地设计.md)**（Windows / 暗区突围：无限 / 图标悬浮窗 / 可编辑素材库）。
> 本文档侧重"做什么、怎么识别、坑在哪"，语言无关。

> 目标：一边"听"游戏的声音，一边告诉你"刚才那是什么"（文字/语音/图标），
> 让听不出音效差异的人也能拿到关键信息。

---

## 0. 一句话架构

```
系统音频回环采集(WASAPI loopback)
   → 环形缓冲(回溯用) → 短窗特征(log-mel patch)
   → 与"音效模板库"匹配/分类 → 事件去抖 + 时间逻辑
   → 悬浮窗文字 / 语音 / 日志
```

### 最关键的一个判断

游戏里"触发式音效"（技能、爆头、Boss 转阶段、拾取、警报）**每次播放都是录音级完全相同的样本**，
不是自然环境里那种每次都不一样的声音。所以：

- **第一版不需要训练神经网络**，用"模板匹配"就能做到 90%+ 准确率，且可以随时加/删音效。
- **真正的难点不是识别，而是标注**：你怎么知道"这一段声音 = 游戏里的哪件事"。
  方案里第 2 节给了三条标注路径，其中"从游戏资源文件里直接提取音效"能省掉 80% 的工作量。

---

## 1. 需求拆解与难点

| 环节 | 难度 | 说明 |
|---|---|---|
| 拿到游戏声音 | ★ | Windows 上 WASAPI loopback 即可，不用装虚拟声卡 |
| 建立"声音↔含义"对应 | ★★★★ | 你听不出区别，所以必须靠**游戏内可观察后果**来打标（我死了→"死亡音"） |
| 声音切分/对齐 | ★★ | 音效短（0.2–2 s），需要能量/新颖度触发切分 |
| 识别 | ★★ | 模板匹配足够；背景 BGM 才是主要干扰 |
| 好用的提示 | ★★ | 你听不出，所以提示必须是**视觉**的，别只靠"哔一声" |

---

## 2. 素材获取：三条路径（按优先级）

### 路径 A（首选）：直接从游戏安装目录提取原始音效
拿到原始文件后，**文件名就是标签**，标注成本≈0。

| 引擎 | 特征 | 工具 |
|---|---|---|
| Unity | `*_Data/` 下的 `.assets` / `.resource` / `.bundle` | AssetStudio、AssetRipper |
| FMOD | `*.bank`、`*.fsb` | fmod bank tools、fsbext、python `pyfmodex` 思路 |
| Wwise | `*.pck`、`*.bnk` | wwiser、quickbms + wwise 脚本 |
| Unreal | `*.pak` | FModel、umodel |
| 老游戏/独立游戏 | 直接是散装 `.wav/.ogg/.mp3` | 直接读 |

注意：
- 提取出的音频**可能未经过游戏内后处理链**（Dolby、虚拟环绕、动态范围压缩），
  直接当模板可能匹配不上 → 必须做一次"校准"：在关闭后处理的设置下录一段，用它验证模板。
- 只作个人使用/研究，不要分发提取出的版权素材。

### 路径 B：游戏中录制 + 热键打标（通用兜底）
自己写一个小工具：
- 持续录 loopback，内存里留最近 3 秒环形缓冲；
- 你按下热键（例如 F8）→ 把"最近 3 秒"存成 `templates/<标签>/xxx.wav`；
- 可自动挑出候选：用能量突变 + 谱新颖度自动切出候选片段，你只需"听/看"并命名。

打标时的认知技巧（你听不出也没关系）：
**用游戏内可观察的后果当标签**——"我按了这个键就出现的声音"="技能音"；
"屏幕红了同时有的声音"="受伤/死亡音"；"弹夹数字变化时的声音"="换弹音"。
每次只针对一个事件反复触发 5–10 次，标签就干净了。

### 路径 C：无监督聚类，先发现"一共有几类声音"
不知道游戏里到底有几种音效时：
1. 用预训练音频 embedding（PANNs CNN14 / YAMNet / CLAP / OpenL3）把每个候选片段编码成向量；
2. 用 HDBSCAN / 谱聚类分组 → 得到"这里有 7 种明显不同的声音"；
3. 再逐类回游戏里对照事件命名。
这一步的价值：**先知道有哪几类，再决定要识别哪几类**，避免瞎找。

### 路径 D：外部素材
游戏 Wiki / Fandom 音效列表、YouTube "all sound effects" 合集、拆包社区的资料。
适合做"参考库"和交叉验证。

---

## 3. 采集层（Windows）

- **推荐库**：`soundcard`（一行代码拿 loopback）或 `PyAudioWPatch`。
  ```python
  import soundcard as sc
  spk = sc.default_speaker()
  mic = sc.get_microphone(id=str(spk.name), include_loopback=True)
  with mic.recorder(samplerate=48000, channels=2) as rec:
      data = rec.record(numframes=960)   # 20 ms
  ```
- 你机器上已探测到的输出端点：
  `Realtek(R) Audio`、`AMD High Definition Audio Device`、`High Definition Audio Device`。
  → 如果游戏声音走 HDMI/显示器/电视，务必在配置里**显式选择端点**，别用"默认设备"。
- 参数建议：48 kHz、双声道 → 下混单声道、块长 20 ms、环形缓冲 2–3 s。
- **必须关闭的音频后处理**（否则波形变了，模板失配）：
  游戏内"夜间模式/音量均衡"、Windows 空间音效、Dolby/DTS、耳机虚拟环绕、驱动里的 EQ。
- 排他模式（WASAPI Exclusive）的少数游戏抓不到 → 兜底方案：
  VB-Cable 虚拟声卡（游戏输出到 CABLE Input），或 OBS 的 "Application Audio Capture" 转发。
- 蓝牙耳机延迟 100–300 ms：识别依旧准，只是提示更晚；要"实时反应"就上有线。
- 音量自适应：滑窗 RMS 归一化，避免你调音量后模板全部失配。

---

## 4. 特征层

- 主特征：**log-mel 谱图 patch**，例如 64 mel × 32 帧（`n_fft=1024`、`hop=256` @48k，≈5.3 ms/帧，覆盖 ~170 ms）。
- 辅助特征：MFCC、谱质心/带宽/滚降、过零率、chroma（用于区分"金属撞击 vs 低频轰鸣"这类）。
- 归一化：每窗峰值/RMS 归一 → 抗音量；谱减/维纳滤波 → 抗背景 BGM。
- **BGM 抵消**（模板匹配能否在战斗中生效的关键）：用长时分位数估计背景谱底，做谱减后再匹配。
- 模板存储：每类保存对齐后的 log-mel patch 集合 + 均值，时移由滑窗最大化相似度或 DTW 处理。
  `templates/<label>/` 放 wav，`templates/index.npz` 放特征与 meta.json（采样率、增益、来源）。

---

## 5. 识别层：三档，逐级升级

| 档 | 方法 | 何时用 | 成本 |
|---|---|---|---|
| **L1 模板匹配（MVP）** | 滑窗余弦/相关，取 top-k，阈值 + top1/top2 margin 判定 | 有原始音效或录制样本时，**先做这个** | 极低，无需训练，可即时增删 |
| **L2 小 CNN/CRNN** | 用样本 + 增强（随机音量、混入 BGM、±5% 拉伸、EQ）合成数据集训练多标签分类，导出 ONNX | 模板失配、或音效有随机变体（连发、远近）时 | 中，2–4 天 |
| **L3 预训练零样本** | CLAP / YAMNet 文本提示（"gunshot"、"level up"） | 完全没有素材时粗分类、或给未知声音起名参考 | 低，但准确率一般，不做主方案 |

关键细节：
- **多标签**而不是 softmax 单选：一帧里可能同时有脚步声 + 换弹声。
- 每个类别独立阈值 + 自适应阈值（近 N 分钟分数的分位数），比固定阈值稳得多。
- 推理预算：20 ms 音频 + 5 ms 特征 + 2 ms 匹配 + 去抖 ≈ **端到端 50–100 ms**，完全够用。

---

## 6. 事件层（决定"好不好用"）

- 峰值检测：相似度曲线上的局部极大 + 阈值。
- 去抖：同一音效 200–500 ms 冷却，避免 BGM 段落反复触发。
- 时间逻辑 / 状态机：把音效组合成**游戏语义**，用规则而不是硬编码进分类器：
  - `脚步 + 换弹` → "附近有人"
  - `低频轰鸣持续 3 s` → "Boss 转阶段"
  - `警报音 + 无我方位移` → "有人绕后"
- 未知事件队列：低于阈值的候选片段存下来，事后聚类 → 提醒你"可能发现了新音效"。

---

## 7. 输出层（人机界面）

- **悬浮窗**（置顶、可鼠标穿透、可缩放）：
  - 大字显示当前事件名 + 图标 + 置信度条；
  - 右侧最近 5 条事件时间线（带时间戳）；
  - 用色块区分类别（你听不出，视觉编码必须够强）。
- 可选提示方式：TTS 语音（pyttsx3 / Edge TTS）、每种音效不同的蜂鸣节奏、屏幕边缘闪光、手机推送。
- **日志**：CSV/JSONL 记录 `时间戳, 类别, 分数, 快照wav` → 事后复盘误报/漏报，同时也是后续训练集的标注来源。
- 调试面板：实时谱图 + 相似度曲线，必要时人工看对齐情况。

---

## 8. 技术栈与目录结构

- Python 3.10.11（本机已装）或 3.11，用 venv 隔离。
- 依赖：`soundcard` / `PyAudioWPatch`、`numpy`、`scipy`、`librosa`、`soundfile`、`scikit-learn`；
  可选 `onnxruntime`（L2）、`PySide6` 或 `FastAPI + WebSocket + 浏览器悬浮窗`（更省事）。
- 单进程多线程：采集线程 → 环形缓冲 → 推理线程（队列）→ UI 线程（Qt 信号 / WebSocket）。

```
sound_check/
  capture/    loopback.py  ring_buffer.py  resample.py
  features/   logmel.py  vad.py  bgm_suppress.py
  match/      templates.py  similarity.py
  events/     detector.py  rules.py
  ui/         overlay.py            # 或 web/ (FastAPI + 前端悬浮窗)
  tools/      record_label.py       # 热键打标
              extract_assets.py     # 从游戏资源提取音效
              build_templates.py    # 生成模板索引
              cluster_unknown.py    # 未知音效聚类
  templates/  <label>/*.wav  index.npz  meta.json
  data/       recordings/  logs/
```

---

## 9. 分阶段路线图（每阶段都有能跑的东西）

| 阶段 | 内容 | 时间 | 验收标准 |
|---|---|---|---|
| P0 | 抓 loopback、保存 wav、能看谱图 | 0.5 天 | 录下一段游戏战斗音频文件 |
| P1 | 热键打标工具 + 候选自动切分 | 1 天 | 每个关键音效 ≥5 条样本 |
| P2 | 模板匹配 + 终端实时提示 | 1–2 天 | 已知音效 top1 ≥90%，误报 <1 次/分钟 |
| P3 | 悬浮窗 + 时间线 + CSV 日志 | 1–2 天 | 能全程挂着打一局，不打扰操作 |
| P4 | 规则/状态机（组合语义提示） | 按需 | "附近有人""Boss 转阶段"类提示生效 |
| P5 | 若准确率不足 → CNN + 数据增强 | 2–4 天 | top1 ≥95%，泛化到不同局面 |

**MVP 定义（一周内可用）**：loopback + 热键打标 + 64×32 log-mel 模板匹配 + 小悬浮窗 + CSV 日志，
只覆盖 5–8 个你最需要的关键提示音，准确率目标 ≥90%。

---

## 10. 风险与坑（逐条）

1. **音频后处理**：Dolby / 空间音效 / 夜间模式 / 动态压缩会改变波形 → 模板失效。必须关闭；
   关不掉就用"同一处理链下录制的音频"当模板（因此提取的原始素材要经过一次在游戏内校准）。
2. **独占模式** → loopback 静音；用 VB-Cable 或 OBS Application Audio Capture 兜底。
3. **多输出设备 / 热切换**：端点做成配置项，并支持"跟随默认设备"。
4. **反作弊与规则**：本方案**只读系统音频输出**，不做进程注入、不读内存、不改游戏文件，
   技术上风险极低；但悬浮窗必须是独立进程的普通置顶窗口（注入游戏进程是绝大多数封号红线）。
   竞技类游戏请自行确认是否允许"音频提示类辅助工具"。
5. **采样率/时钟漂移**：长时间运行需定期重同步。
6. **训练/测试泄漏**：同一段录音切出来的样本会让准确率虚高 → 按"游戏局次"划分训练/测试。
7. **阈值漂移**：场景音量差异大 → 自适应阈值 + 用户可调灵敏度。
8. **别贪多**：先只做 5–10 个关键音效，价值/工作量比最高；覆盖 100 种音效是不同量级的工程。

---

## 11. 需要你确认的 3 件事

1. **游戏名 / 引擎**是什么？（决定能否走"路径 A 拆包拿原始音效"，这条能省掉 80% 标注工作）
2. 游戏声音输出到**耳机 / 显示器 / 电视**？游戏是否开了"夜间模式/空间音效"等后处理？
3. 你想要哪种提示：**屏幕文字悬浮窗** / **语音播报** / **两者都要**？

确认后我就按 P0 → P1 开始搭，第一个交付物是"能录下游戏声音并打标"的工具。
