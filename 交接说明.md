# 交接说明（SoundRadar 项目现状）

> 用途：换电脑 / 换会话后，**让新的 AI 会话或你自己在 5 分钟内接上进度**。
> 最后更新：`0.6.0-p4`（P0–P4 全部完成并验证）。
> 配套：[`使用手册.md`](使用手册.md)（怎么用）、[`新电脑部署.md`](新电脑部署.md)（怎么搬）、
> [`方案-落地设计.md`](方案-落地设计.md)（设计与原理）、[`录音与打标指南.md`](录音与打标指南.md)（你手上的活）。

---

## 1. 这是什么

一个 **Windows 小工具（纯 Go、无 cgo、单文件 exe 约 8.6 MB）**：
一边"听"系统输出声音（WASAPI loopback），一边告诉你"刚才那是什么"，**在屏幕上弹出对应图标**。

给谁用：玩《暗区突围：无限》但听不出音效区别的人（用户本人）。
核心思路：**把听觉辨识变成视觉辨识**——把游戏音效的 log-mel 指纹与自建样本库比对。

**关键判断（决定了整个方案）**：游戏触发式音效每次播放都是"录音级相同"的样本，
所以**不需要训练神经网络**，模板匹配就能到 0.99+；难点不在识别，在**建立"声音↔含义"的标注**。

---

## 2. 当前状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | WASAPI loopback 采集 → WAV（`devices` / `capture`） | ✅ 端到端验证（880 Hz PASS、负对照 FAIL） |
| P1 | 素材库 `library.srz`（ZIP）+ 管理端（上传/命名/配图标） | ✅ 真实 HTTP + 重启持久化验证 |
| P2 | 自写 FFT + log-mel + int8 量化指纹库 + 匹配门控 | ✅ 匹配 ≈1.00 / 非匹配 ≈-0.05 |
| P2 | 实时链路 `live` + SSE + 管理端「实时打分」 | ✅ 真声卡边播边采，CPU 0.16% |
| P3 | 原生 Win32 悬浮窗（图标 + 坐标可调 + 鼠标穿透 + F9） | ✅ **截图取证**（修过一个致命 bug，见 §5） |
| P4 | F8 回溯保存 + 候选项收件箱 + 一键入库 | ✅ 独立验证（存下来的音频经 `wavstat` 判定 880 Hz PASS） |
| P5 | **装备档案（头盔/耳机）+ 离群样本清理 + 数据增强** | ⏳ 下一步 |

**版本 `0.6.0-p4`**，10 个子命令：`devices` `capture` `serve` `version` `index` `match` `live` `overlay` `recall` `help`。

---

## 3. 关键实测数字（"它真的能用"的证据）

| 指标 | 实测 |
|---|---|
| 特征 | 48 kHz / 帧长 1024 / 跳步 256（5.333 ms）/ Hann / 64 Mel / 窗 32 帧（186.7 ms）= **2048 维** |
| FFT 精度 | 与自己写的朴素 DFT 最大误差 **6.2e-11** |
| int8 量化 SNR | **42.55 dB** |
| 检索耗时 | 1600 模板 × 2048 维 → **2.26 ms**（边界性能测试有时 3–4.5 ms，非阻塞问题） |
| 流式一致性 | 非对齐分块 vs 一次性分析 **逐元素差 0.000e+00** |
| **匹配分离度** | 真实命中 **0.995–1.0000**，非命中 **-0.06–-0.04** |
| 实时开销 | 每 20 ms 音频 **0.045–0.104 ms**（预算 10 ms）、CPU **0.16%**、堆 3 MiB |
| 内存有界 | 10 分钟音频后 HeapInuse 不增长（曾发现 659 MiB/小时的泄漏，已修） |
| 回溯保存 | 2.98 秒候选，`wavstat` 判定 880.0 Hz、负对照 1500 Hz 为 0% |

---

## 4. 文件地图

```
sound_check\
  使用手册.md            怎么用（命令、工作流、FAQ）
  录音与打标指南.md      你要做的录音工作 + 命名规范
  方案-落地设计.md       设计与原理（含 ABI 的四个坑、P3 的 bug 复盘）
  方案.md / 方案-go实现.md  更早的通用方案与 Go 选型分析
  交接说明.md            本文件
  新电脑部署.md          换机步骤
  迁移打包.ps1           打迁移包

  soundradar\
    bin\soundradar.exe       主程序（唯一需要的文件）
    bin\wavstat.exe          验证小工具（生成测试音 / 分析 WAV / Goertzel）
    bin\data\library.srz     你的音效库（ZIP，**要备份**）
    bin\data\index.bin       指纹索引（可重算）
    bin\data\candidates\     F8 抓的候选项（消费后即删）
    bin\config.json          设置（悬浮窗坐标、端点、热键、回溯时长）
    build.ps1                构建（含 SAC 换哈希重试）——**构建一律用它**
    internal\dsp\            自写 FFT / Mel / 流式 Analyzer
    internal\index\          量化指纹库 + index.bin 格式 + Search
    internal\match\          阈值/margin/静音/冷却/全局不应期 五重门控
    internal\live\           实时链路（采集解耦、内存有界、耗时统计）
    internal\overlay\        Win32 分层窗口 + 纯 Go 渲染器
    internal\recall\         回溯环形缓冲 + 候选项存储
    internal\hotkey\         全局热键（隐藏消息窗口，RegisterHotKey）
    internal\server\         HTTP API + 内嵌管理端（4 个页签）
    internal\capture|wav|audio|library|config|testaudio
    cmd\soundradar\          各子命令；cmd\wavstat\
    artifacts\               验收证据（截图、CSV、报告）
    verify_*.ps1 / *_test.go 验收脚本与单测
```

---

## 5. 血泪坑清单（**接手的人一定要看**）

1. **验证 GUI 只能认"屏幕截图里有像素"**。P3 的悬浮窗曾经：ULW 返回成功、`IsWindowVisible=true`、
   样式全对、`GetWindowRect` 对、自己 dump 的帧缓冲内容也对——**但屏幕上什么都没有**。
   根因：`UpdateLayeredWindow` 的 `psize` 是 `SIZE`（两个 **32 位** LONG），原代码把尺寸
   打包成 `uint32(h<<16|w)` 传进去，于是窗口变成 **700 万像素宽 × 0 像素高**。
   （同时它每帧把窗口矩形写坏成 `R=7080424 B=1260`，之前有人为此打了"每帧 SetWindowPos"的补丁——
   那是给假象打补丁。）**教训：返回码/状态/dump 都不算证据，截图才算。**
2. **重复预乘 alpha**：`image.RGBA` 本身就是预乘的，写 DIB 时只需 RGBA→BGRA 重排，别再乘一次。
3. **绝对不要 `SendInput`/`keybd_event`/`SetWindowsHookEx`**：和 ACE 反作弊同时跑，模拟输入是封号红线。
   热键只用官方 `RegisterHotKey`，验证只验"注册成功 + 内部触发有效"。
4. **Smart App Control 按文件哈希拦未签名 exe**（本机实测过 `0x8000FFFF`/`Access is denied`/直接拒绝启动）。
   对策：`build.ps1` 会"编译→启动自检→被拦就换 buildSalt 重编"，最多 6 次。
5. **沙箱/权限**：Go 默认 `GOPATH/GOCACHE` 在 workspace 外会被拒 → `build.ps1` 全部指到工程内；
   `proxy.golang.org` 不通 → 必须 `GOPROXY=https://goproxy.cn,direct`；`go env -w` 在沙箱里会失败。
6. **`±Inf` 不能进 JSON**：静音 `-Inf dBFS`、无第二名 `+Inf margin` 会让 `encoding/json` 失败并返回**空 200 body**。
   所有浮点 DTO 用 `*float64`，静音输出 `null` + `silent:true`。
7. **PowerShell 5.1**：写含中文的 `.ps1` 必须 **UTF-8 带 BOM**；`Set-Content -Encoding UTF8` 写的 BOM 会让
   `encoding/json` 解析失败；`Start-Process -RedirectStandardOutput` 在沙箱里被拒；
   `& $exe -test.v` 会被吃掉参数（用 `Start-Process -ArgumentList` 或 `--%`）。
8. **`dsp` 曾把全部历史采样留在内存**（659 MiB/小时）→ 用 `Analyzer.Trim()` 修；离线路径不受影响。
9. **重采样不做抗混叠**（线性插值，有意的取舍）；入库统一 48k/16-bit/mono 并保留 `origin` 原始格式。
10. **`log-mel` 地板 = -20 dB**（不是教科书的 -100 dB）。取 -100 时未注册声音会拿到 0.8157（无区分度），
    取 -20 后降到 0.2345 且带噪识别从 0.549 升到 0.892。**这个值已进特征指纹，改动即不兼容**（索引自动重建）。
11. **别引入新依赖**：目前只有 `go-ole`、`go-wca`、`x/sys`、`go-mp3`、`x/image`（+ 传递的 `x/text`）。

---

## 6. 命令速查

```powershell
cd <工程>\soundradar

.\bin\soundradar.exe version                    # 版本 / buildSalt / 是否纯 Go
.\bin\soundradar.exe devices                    # 音频输出端点
.\bin\soundradar.exe capture --seconds 10 --out D:\sr\样本.wav
.\bin\soundradar.exe serve --open [--overlay]    # 管理端（音效库/实时打分/候选项/设置）
.\bin\soundradar.exe index rebuild              # 重建指纹索引
.\bin\soundradar.exe match --wav 录音.wav --top 5   # 这段录音最像哪个音效
.\bin\soundradar.exe live --csv 命中.csv         # 终端实时面板
.\bin\soundradar.exe overlay [--demo] [--x N --y N]  # 打游戏时跑的那个（F8 回溯、F9 显隐）
.\bin\soundradar.exe recall --seconds 8 --at 3 --out t.wav   # 不按键触发一次回溯（验证用）

.\build.ps1 -Test        # 构建 + 自检 + vet + 单测（**一律用它**）
```

---

## 7. 下一步（P5）与真正的前置条件

**P5 的计划**：
1. **装备档案（Profile）**：`不戴 / 头盔 / 耳机` 三套独立模板与阈值。
   根因：防暴头盔是**全局低通+降音量**、战术耳机改**最大衰减距离**，同一音效听感完全不同→
   不分档案的话，换个装备准确率就崩。这是 ABI 场景里最关键的一条。
2. **离群样本清理**：自动标出离样本中心最远的样本，一键剔除录错的。
3. **空间化数据增强**：低通（模拟距离/遮挡）、混响、±3% 变速、±6 dB 增益 → 提升 3D 音效召回。
4. 可选：L2 小 CNN（Go 手写推理）、库导出分享、SIMD 优化检索、`net/http` 换手写最小 HTTP（省 1–2 MB）。

**⚠️ 前置条件：需要真实游戏样本。** 装备档案/阈值调优**没有真实音频就只能拿合成音猜**。
所以正确的顺序是：

1. 用户按 [`录音与打标指南.md`](录音与打标指南.md) 录 4 类（脚步/换弹/开枪/开门）× 5 条，
   刻意覆盖近-中-远、是否隔墙；**录音前关掉所有音频后处理**（这是失配的头号原因）；
2. 用 `match` 核对每条分数（>0.9 干净、<0.75 重录）；
3. 开一局 `overlay`，用 F8 攒样本，观察误报；
4. 把"哪类准、哪类爱误报、戴不戴头盔差多少"这些**真实数字**带回来 → 再做 P5。

---

## 8. 给新会话的 AI 的开场提示（可直接粘贴）

```
这是一个 Windows 游戏音效识别工具（Go，纯 Go 禁 cgo，只做 Windows），
工程在 <工程路径>，当前版本 0.6.0-p4，P0–P4 已完成并验证。
请先读 交接说明.md（项目现状与坑清单）、使用手册.md（用法）、方案-落地设计.md（设计与原理），
再读 soundradar\ 下的源码。构建一律用 soundradar\build.ps1（自带 Smart App Control 换哈希重试）。

关键约束：
1) 验证 GUI 只能认"屏幕截图里有像素"，返回码/IsWindowVisible/自己 dump 的缓冲都不算证据；
2) 绝不使用 SendInput/keybd_event/SetWindowsHookEx（与游戏 ACE 反作弊冲突）；
3) 不加新的第三方依赖；Go 的 cache 必须指到工程内，GOPROXY 用 https://goproxy.cn,direct；
4) 任何"验证通过"的结论都要贴真实输出，做不到的必须明说。

下一件事：<例如：等我先录 20 条真实样本，然后做 P5 的装备档案>
```
