# SoundRadar 使用手册

> 一边听游戏的声音，一边告诉你"那是什么"。
> 当前版本：`0.7.1-p5`（纯 Go，无 cgo，单文件 exe 约 9 MiB）
> 录音打标方法见 [`录音与打标指南.md`](录音与打标指南.md)。

---

## 0. 最快上手：双击 exe

**双击 `bin\soundradar.exe`**（不带任何参数）就会：

1. 在系统托盘放一个雷达图标；
2. 启动 `serve --overlay`：管理页面 + 实时识别 + 命中悬浮窗；
3. 弹出气泡提示，告诉你管理界面地址。

托盘图标怎么用：

| 操作 | 效果 |
|---|---|
| 左键单击 / 双击 | 在浏览器打开管理界面 |
| 右键 | 弹出菜单 |

菜单项：打开管理界面、查看实时识别状态、显示/隐藏命中悬浮窗、打开候选项目录、打开程序数据目录、**退出 soundradar**（一起停掉识别、悬浮窗和托盘图标）。

重复双击不会起第二个服务：端口已经在监听时直接复用，只再打开一个页面。**托盘区始终只有一个图标**——管理端进程本身不注册图标，由这个启动器统一负责（否则会出现两个图标和两个"退出"入口）。

如果启动器被任务管理器强杀，它带的 serve 子进程也会一起结束（Windows 作业对象），不会留下一个没有图标的采音进程。

命令行用法完全没变（见第 3 节）。想让命令行版本也带托盘图标，给 `serve` 加 `--tray` 即可。

---

## 1. 它现在能做什么

| 能力 | 状态 |
|---|---|
| 双击 exe 直接启动（托盘图标 + 管理端 + 实时识别 + 悬浮窗） | ✅ |
| 抓取系统输出声音（WASAPI loopback，不需要虚拟声卡） | ✅ |
| 环境音过滤（高通 + 逐频点信噪比抑制 + 自适应门限），游戏有杂音时也认得出 | ✅ |
| 录入音效：上传音频 + 起名 + 配图标（可多样本变体） | ✅ |
| 计算音频指纹并建索引（int8 量化，200 条库只要 200 KB 左右） | ✅ |
| 离线匹配："这段录音最像哪个音效？" | ✅ |
| 实时识别：终端面板 / 实时打分页 / CSV 日志 | ✅ |
| 命中时弹出图标悬浮窗（原生 Win32，置顶、鼠标穿透、不抢焦点） | ✅ |
| 悬浮窗坐标/大小/不透明度/时长可调（设置页 + config.json，改完热生效） | ✅ |
| 按热键（F8）回溯保存"刚才那 3 秒" → 候选项收件箱 → 命名配图标一键入库 | ✅ |
| 装备档案（头盔/耳机分别建库）、离群样本清理 | ⏳ 待做 |

**实测性能**：匹配分数 真实命中 ≈ 1.00 / 非命中 ≈ -0.05（分离度极大）；
每 20 ms 音频处理耗时 0.045–0.104 ms（预算 10 ms）；CPU 约 0.16%；内存堆 3 MiB 左右。

---

## 2. 文件在哪

```
soundradar\
  bin\soundradar.exe        ← 主程序（唯一需要的文件）
  bin\wavstat.exe           ← 开发/验证用小工具（生成测试音、分析 WAV）
  build.ps1                 ← 构建脚本（编译 + 启动自检 + 绕过 Smart App Control）
  config.json               ← 设置（自动生成；和库分开，换库不丢设置）
  data\library.srz          ← 你的音效库（ZIP 容器，建议定期备份！）
  data\index.bin            ← 指纹索引（可随时重算，不用备份）
  artifacts\                ← 测试与验证的过程产物
```

- **`library.srz` 是 ZIP**，用任何解压软件都能打开看：`manifest.json` + `items/<id>/{meta.json, icon.png, samples/*.wav}`。
  原始样本音频一律保留，所以以后改特征参数可以一键重算全部向量，不会把数据做死。
- 想换库位置：所有命令都支持 `--library <路径>`。

---

## 3. 命令速查

```powershell
cd soundradar
```

| 命令 | 作用 |
|---|---|
| `.\bin\soundradar.exe`（双击，或直接运行不带参数） | **日常用这个**：托盘图标 + 管理页面 + 实时识别 + 悬浮窗 |
| `.\bin\soundradar.exe tray [--port N] [--no-open] [--no-serve]` | 同上（显式写法）。`--no-open` 不自动开浏览器，`--no-serve` 只放托盘图标 |
| `.\bin\soundradar.exe version` | 看版本/构建信息（含 buildSalt、是否纯 Go） |
| `.\bin\soundradar.exe devices` | 列出音频输出端点，标出默认端点 |
| `.\bin\soundradar.exe devices --probe` | 逐个端点试着打开一次回环采集，报告哪些能用（排查 `IAudioClient::Initialize` 报错用） |
| `.\bin\soundradar.exe capture --seconds 10 --out D:\sr\样本.wav [--device Realtek]` | 录系统输出声音到 WAV |
| `.\bin\soundradar.exe serve [--port 8765] [--open] [--library 路径] [--overlay] [--tray]` | 打开管理端（浏览器访问 127.0.0.1）；加 `--overlay` 同时开启悬浮窗，加 `--tray` 同时放托盘图标 |
| `.\bin\soundradar.exe index rebuild [--library 路径] [--out 路径]` | 从库重建指纹索引 |
| `.\bin\soundradar.exe match --wav 录音.wav [--top 5] [--min-score 0.75] [--all] [--json]` | 离线识别：这段录音最像哪个音效 |
| `.\bin\soundradar.exe live [--device SUBSTR] [--library 路径] [--seconds N] [--top 8] [--csv 命中.csv] [--json] [--quiet] [--wav 文件]` | 实时识别（终端面板 + 可选 CSV/JSONL） |
| `.\bin\soundradar.exe overlay [--x N --y N] [--size N] [--demo] [--hold-item ID --hold-seconds N] [--wav 文件] [--seconds N] [--csv 命中.csv]` | **打游戏时跑的那个**：采集 + 识别 + 命中弹图标 + F8 回溯保存 |
| `.\bin\soundradar.exe live --recall` | 终端面板模式同时启用 F8 回溯保存 |
| `.\bin\soundradar.exe recall [--seconds N] [--at SECONDS] [--out PATH] [--json]` | 不依赖按键触发一次回溯保存（自动化验证/手动抓样本用） |

`live` / `overlay` 的 `--wav` 是**离线回放**（不需要声卡，便于复现问题）；加 `--realtime` 按真实速度播。
`overlay --demo` 不进采集，循环展示库里每个条目（每个 1.5 秒）——**第一次用建议先跑这个看效果、调坐标**。

---

## 4. 五步工作流（从零到能用）

### 第 0 步（可选）：直接双击试试

双击 `bin\soundradar.exe`，等托盘区出现雷达图标，浏览器会自动打开管理界面。想确认悬浮窗长什么样，可以在命令行跑 `.\bin\soundradar.exe overlay --demo`。

### 第 1 步：录音
```powershell
.\bin\soundradar.exe devices          # 先确认声音走哪个端点
.\bin\soundradar.exe capture --seconds 10 --out "D:\sr\换弹-近-无遮挡-耳机-01.wav"
```
**一次只录一个事件**，用游戏里的可见后果确认时刻（按了 R 之后那声 = 换弹）。
建议**找一段游戏相对安静的时刻录模板**：模板里的环境音越少，之后在嘈杂场景里越容易认出来（实时识别那一侧有环境音过滤兜着）。
细节看 [`录音与打标指南.md`](录音与打标指南.md)。

### 第 2 步：入库 + 配图标
```powershell
.\bin\soundradar.exe serve --open
```
浏览器打开后：
- **音效库**页签 → 拖入音频 → 填名称 → 传图标（正方形 PNG + 透明背景最好）→ 保存；
- 同一个音效的多个变体（近/远/隔墙、戴不戴头盔）**追加到同一个条目**（详情页里有"追加样本"）；
- 阈值/冷却时间逐条可调，默认 0.72 / 400 ms。

### 第 3 步：建索引
```powershell
.\bin\soundradar.exe index rebuild
```
（`match` 和 `live` 在发现索引缺失/过期时会自动重建，这一步通常可以跳过。）

### 第 4 步：验证样本质量
```powershell
.\bin\soundradar.exe match --wav "D:\sr\换弹-近-无遮挡-耳机-01.wav" --top 5
```
| 输出 | 含义 | 处理 |
|---|---|---|
| 分数 > 0.9，第一名是本体 | 干净样本 | 直接入库 |
| 0.75–0.9 | 可用 | 建议和其他变体一起注册 |
| < 0.75 或第一名是别的类别 | 这段混了 BGM/枪声/语音 | 重录或裁短 |

### 第 5 步：实时跑
```powershell
# 终端面板 + CSV 日志
.\bin\soundradar.exe live --csv "D:\sr\命中.csv"

# 或者用管理端的「实时打分」页签（推荐：看得见柱子）
.\bin\soundradar.exe serve --open
```
终端面板长这样：
```
[live] 15:26:14.221     -4.5 dBFS  880Hz 测试音 0.578 | 1500Hz 测试音 -0.038
[live] ★ 15:26:14.266  命中「880Hz 测试音」 分数 0.7653  margin 0.8132  电平 -4.47 dBFS
```

### 第 6 步（正式用）：开悬浮窗打游戏
```powershell
# 先看效果 + 调坐标（循环展示库里每个条目，不需要游戏、不需要声卡）
.\bin\soundradar.exe overlay --demo --x 2236 --y 1260

# 正式用：采集 + 识别 + 命中弹图标（不需要开浏览器）
.\bin\soundradar.exe overlay

# 想同时开管理端就加 --overlay
.\bin\soundradar.exe serve --overlay --open

# 最省事：双击 bin\soundradar.exe（= 托盘图标 + 管理端 + 悬浮窗）
```
悬浮窗行为：
- 命中时在指定位置弹出**图标 + 名称 + 置信度百分比 + 分数条**，淡入 60 ms / 保持 1150 ms / 淡出 250 ms；
- **鼠标穿透**（打游戏时不会挡住点击）、**置顶**、**不抢焦点**（不会让游戏失焦）、不出现在任务栏/Alt-Tab；
- 最多同时显示 3 个不同音效；**同一个音效连续命中只刷新那一个格子**（不会挤掉别的）；
- `F9` 显示/隐藏悬浮窗（应急开关，可在设置页改键）；
- 名称太长会自动截断成「xxx …」——**嫌名字显示不全就把 `--size` 或设置页里的格子尺寸调大**（例如 128），或者关掉"显示名称"只留图标。

### 第 7 步：打游戏时用 F8 攒样本（这是最省事的录入方式）
```powershell
.\bin\soundradar.exe overlay        # F8 默认已启用
```
听到一个不认识的声音 → **按一下 F8** → 终端打印：
```
[recall] 已保存候选项 000001-20260918-094537（3.0 秒，峰值 -0.83 dBFS，猜测：880Hz 测试音 1.00）
```
然后打开管理端 →「**候选项**」页签：
- 每条可以**试听**（看到时长、峰值、以及**保存那一刻系统猜的是什么、分数多少**）；
- 填名称、标签、备注、上传你的图标 → 选「**新建条目**」或「**追加到已有条目**」→ 保存；
- 保存后候选项消失、条目出现在「音效库」里，样本直接可用（不用再手工裁剪）。
- 页签上的**徽标**显示还有多少条没处理；也可以直接点「立即保存最近 N 秒」按钮手动抓一次。

> 回溯窗口默认 3 秒（`recall.seconds`）。如果那个声音来得快去得也快，把它调到 5–8 秒，按 F8 时"刚才"的覆盖范围就更大。

---

## 5. 管理端四个页签

- **音效库**：图标网格 / 搜索 / 新增条目（拖拽音频 + 名称 + 图标）/ 详情（试听样本、改阈值冷却、追加变体、删除）。
- **实时打分**：设备下拉、开始/停止、大字电平条、top-N 横向分数条、命中时间线。
  **这是给"听不出区别"的人的核心工具**：开着游戏走一圈，看哪根柱子飙起来，就知道这条是什么声音。
- **候选项**（P4）：F8 抓下来的片段都在这里——试听、起名、配图标、一键入库（新建条目或追加到已有条目）；顶部有「立即保存最近 N 秒」按钮；页签带待处理数量徽标。
- **设置**：采集端点选择、**悬浮窗 X/Y 坐标**与显示器、大小/不透明度/显示时长/最多同时显示个数/是否显示名称分数、预览按钮、显示隐藏开关、热键显示、回溯保存开关与时长。
  改完**立刻生效**（悬浮窗马上移动、回溯时长立刻改变），并写回 `config.json`。
  当前生效的环境音过滤参数也会在这里列出来（改它需要编辑 `config.json` 的 `noise` 段，见第 6 节）。

实时面板底部还会显示累计统计：块数、音频秒数、窗口数、tick、命中数、**丢帧数**、单块平均/峰值耗时、每 20 ms 音频耗时、堆内存。
**丢帧或超预算会变红告警**——正常情况下应该一直是 0 和绿色。

---

## 6. config.json

```json
{
  "capture": { "device": "Realtek(R) Audio", "fallbackToDefault": true },
  "overlay": { "enabled": true, "monitor": 0, "x": 1812, "y": 984, "anchor": "bottom-right",
               "size": 96, "opacity": 0.85, "durationMs": 1150, "fadeInMs": 60, "fadeOutMs": 250,
               "maxSimultaneous": 3, "showName": true, "showScore": true, "margin": 24 },
  "hotkeys": { "toggleOverlay": "F9" },
  "profile": "default"
}
```
- `x`/`y` 是**物理像素**；留空（0）时按 `anchor` 九宫格 + `margin` 自动摆放。
- 改完**热生效**（悬浮窗立刻移动，采集端点立刻切换），不用重启。

P4 新增：
```json
"hotkeys": { "toggleOverlay": "F9", "recallLabel": "F8" },
"recall":  { "enabled": true, "seconds": 3, "dir": "data/candidates", "maxFiles": 200 }
```
- `recall.seconds` 是回溯窗口（1–30 秒，默认 3）；`maxFiles` 是收件箱上限（超过就删最旧的，默认 200）。
- `dir` 相对路径按 exe 目录解析（和 `library.srz` 同规则）。

P5 新增（环境音过滤）：
```json
"noise": {
  "method": "subtract",   // subtract = 高通 + 逐频点抑制；highpass = 只做高通；off = 全关
  "highPassHz": 120,      // 高通截止频率（去掉隆隆声/风噪）
  "strength": 1.5,        // 过减系数：越大压得越狠
  "gainFloorDb": -8,      // 单个频点最多被压掉多少 dB
  "adaptiveGate": true,   // 静音门限跟着实测噪声底走
  "gateMarginDb": 4,      // 门限比噪声底高多少 dB
  "gateFloorDbfs": -70    // 自适应门限的下限（更安静时仍用 -60 dBFS）
}
```
- 这些参数属于**指纹**的一部分：改动后下次开始识别会自动用 `data\library.srz` 里的原始音频重算 `data\index.bin`（库文件不受影响，只是第一次启动会多等一会儿）。
- 认不出时先别急着改这里。默认值在“干净录音”和“强环境音”两头都验证过：干净录音自相似度 > 0.999，强环境音下相似度 0.63 → 0.84、与干扰音的差距 +0.14 → +0.31。

---

## 7. 常见问题

**Q：双击 exe 提示 "An Application Control policy has blocked this file"？**
本机 Smart App Control 会按**文件哈希**拦截未签名的自制程序，且不稳定（同一个源码这次能跑下次被拦）。三个选择：
① 用 `.\build.ps1` 重新构建（它自带"启动自检 + 换哈希重试"，默认试 6 次）；
② 关闭 Smart App Control（设置 → 隐私和安全性 → Windows 安全中心 → 应用和浏览器控制 → 智能应用控制 → 关闭）。
⚠️ **关掉是单向操作，想再开必须重装/重置 Windows**；③ 买代码签名证书。

**Q：`live` 采集不到游戏声音 / 全是静音？**
- 用 `devices` 确认端点，必要时 `--device "Realtek"` 指定；
- 极少数游戏用 WASAPI **独占模式**，loopback 抓不到 → 兜底用 VB-Cable 虚拟声卡；
- 检查 Windows 音量合成器里游戏的音量不为 0。

**Q：报错 `IAudioClient::Initialize(shared/loopback) failed: error 2290679816`？**
2290679816 = `0x88890008` = **AUDCLNT_E_UNSUPPORTED_FORMAT**（Windows 没有这条 HRESULT 的文本，所以后半句才说"找不到消息文本"）。
它的意思是：**这个播放端点拒绝了它自己刚报出来的混合格式**。和你的音量、程序参数都没关系，是端点这一侧的 DSP 链路出了问题。最常见的原因（按概率）：

1. **设备"音频增强 / 音效"被打开**——Realtek/Realtek Audio Console、Nahimic、Dolby Atmos for Headphones、DTS、Sonic Studio 等；
2. **空间音效**开着（Windows Sonic / 杜比全景声 / DTS Headphone:X）；
3. **蓝牙耳机切到了"免提 / 通话"模式**（这一档只有 8/16 kHz，格式和音乐模式完全不同）；
4. **有程序独占占用了它**（部分播放器、直播/录音软件、带 ASIO 的软件）；
5. 驱动或 Windows Audio 服务异常。

**程序已经自动处理的部分**（不用你做什么）：

- **自动换端点**：默认端点打不开时，会按"你指定的/默认 → 另一个角色的默认 → 其余端点"的顺序自动试，第一个能用的就用，并打印说明它换成了哪个、为什么。识别（`serve` / `overlay` / `live`）和录音（`capture`）走的是同一套逻辑。
- **自动换格式**：端点连自己的混合格式都拒绝时，程序会再依次尝试 48 kHz / 44.1 kHz 立体声、单声道等常见格式，能开起来就用（识别链路会自动重采样到 48 kHz）。
- **放宽格式支持**：8/16/20/24/28/32 位整数与 32/64 位浮点都能转换，不会因为"位数奇怪"就放弃。
- **启动就告诉你**：都失败时，启动阶段会直接打印**所有播放端点的清单 + 每个打不开的原因 + 该做什么**（关增强、关空间音效、蓝牙选立体声……）。命令行 `capture` 失败时也会打印同样的一份。
- **采到静音也告诉你**：如果采集成功但连续 20 秒一点声音都没有，会提示"正在监听哪个端点"以及常见原因（游戏输出设备不对 / 独占模式 / 被静音）。

排查步骤：

```powershell
# 1) 看哪个端点能用（会逐个真的打开一次，并显示各自的混合格式）
.\bin\soundradar.exe devices --probe
```

```
OK    [0] 扬声器 (Realtek(R) Audio)
        混合格式: 48000 Hz / 2 ch / 32-bit float
FAIL  [1] 耳机 (Bluetooth Hands-Free)
        混合格式: 16000 Hz / 1 ch / 16-bit PCM
        打不开  : AUDCLNT_E_UNSUPPORTED_FORMAT (0x88890008)：…
```

- 有 `OK`：正常用就行，程序会自己挑能用的那个。想固定用某一个：`--device "<名字片段>"`。
- 全是 `FAIL`：按启动时打印的那份清单逐条改（关增强 → 关空间音效 → 蓝牙切立体声 → 关独占程序 → 查 Windows Audio 服务 → 更新驱动），或用 VB-Cable 虚拟声卡兜底。

> 只有 `--device` 写错（没匹配到端点）时不会打印那份长清单，只会提示正确的名字写法。

**Q：识别不准 / 分数普遍偏低？**
1. **关掉所有音频后处理**：游戏内夜间模式/动态范围压缩、Windows 空间音效、Realtek 响度均衡、杜比/DTS、耳机虚拟环绕——
   这些会改变波形，是失配的头号原因；
2. **环境音太大**：默认已经开了环境音过滤（见第 6 节 `noise`），如果还认不出，可以试着把
   `noise.strength` 从 2.0 提到 2.5–3.0（压得更狠），或者把 `noise.gainFloorDb` 从 -14 放到 -20；
   反过来，如果干净环境下反而变差了，把 `noise.method` 设成 `"highpass"` 或 `"off"` 对比一下；
3. **距离和遮挡会让同一个音效特征完全不同** → 把近/远/隔墙分别录一遍，注册成同一个条目的多个样本；
4. **装备会整体改变听感**（防暴头盔 = 低通 + 降音量，战术耳机改衰减距离）→ 目前需要分档案建库（列在待做的 P6 里）；
5. 调低该条目的阈值，或调大实时面板里的灵敏度。

**Q：双击 exe 之后怎么退出？**
右键托盘区的雷达图标 → 「退出 soundradar」。命令行方式起的进程用 Ctrl+C 或关掉窗口。
托盘菜单里还有「打开管理界面」「打开候选项目录」「打开程序数据目录」等入口。

**Q：误报太多（老是弹同一个）？**
- 提高该条目的阈值（实时面板/详情页都能改）；
- 加长冷却时间（`cooldownMs`）；
- 大概率是这个条目里混进了 BGM 的样本 → 用 `match` 检查每个样本，把低分的删掉。

**Q：`go test ./internal/index/` 报错说 "Application Control policy has blocked this file"？**
那是 SAC 拦了测试二进制，不是代码问题。规避：`go test -c -o artifacts\verify\x.test.exe ./internal/index` 再直接跑那个 exe。

**Q：占用高吗？会影响游戏吗？**
实测 CPU 约 0.16%（16 核口径）、堆内存 3 MiB、峰值工作集 21 MiB，每 20 ms 音频只花 0.05–0.1 ms。基本无感。
悬浮窗一旦交付，会额外加一个置顶窗口（鼠标穿透、不抢焦点），并且**只读系统音频、绝不注入/读内存/模拟输入**。

---

## 8. 安全边界（用暗区突围：无限 时务必知道）

游戏使用 ACE 反作弊（且有"检测到异常"机制）。本工具**只做**：
- 读系统音频输出（WASAPI loopback）
- 画自己的窗口、写自己的库和日志
- 用系统官方 `RegisterHotKey` 注册热键

**绝不做**：注入 DLL / hook 游戏 / 读写游戏内存 / 改游戏文件 / 模拟键鼠输入 / 底层全局键鼠钩子 / 读游戏画面。

**建议**：先在训练场验证 ACE 不报异常，再正常使用；玩的时候用**无边框窗口**模式（独占全屏会把置顶窗口盖住）；
别把悬浮窗摆在准星区域。竞技规则是否允许"音频提示类辅助工具"请自行评估。

---

## 9. 从源码构建

```powershell
cd C:\Users\znz_l\PycharmProjects\sound_check\soundradar
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\build.ps1 -Test          # 编译 + 启动自检 + go vet + go test；被 SAC 拦会自动换哈希重试
```
产物在 `bin\`。构建脚本会把 GOPATH/GOCACHE 指到 workspace 内（沙箱不允许写 `C:\Users\znz_l\go`），
并用 `goproxy.cn` 拉依赖（本机访问不到 `proxy.golang.org`）。
